[project]
name = "backlog_miner"
version = "0.1.0"
description = "A Python library (PDM)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "agent_adapters",
  "backlog_core",
  "runner_core",
]

[build-system]
requires = ["pdm-backend>=2.0.0"]
build-backend = "pdm.backend"

[tool.pdm]
distribution = true

[tool.monorepo]
status = "incubator"

[tool.pdm.scripts]
smoke = "python -m pytest -q tests/test_basic.py"
smoke_extended = "python -m pytest -q tests/test_basic.py"
test = "python -m pytest -q"
lint = "python -m ruff check ."
[tool.pdm.dev-dependencies]
dev = [
  "-e file:///${PROJECT_ROOT}/../../packages/agent_adapters#egg=agent-adapters",
  "-e file:///${PROJECT_ROOT}/../../packages/backlog_core#egg=backlog_core",
  "-e file:///${PROJECT_ROOT}/../../packages/normalized_events#egg=normalized_events",
  "-e file:///${PROJECT_ROOT}/../../packages/reporter#egg=reporter",
  "-e file:///${PROJECT_ROOT}/../../packages/runner_core#egg=runner_core",
  "-e file:///${PROJECT_ROOT}/../../packages/sandbox_runner#egg=sandbox_runner",
  "-e file:///${PROJECT_ROOT}/../../packages/triage_engine#egg=triage_engine",
  "-e file:///${PROJECT_ROOT}/../run_artifacts#egg=run_artifacts",
  "deptry>=0.20.0",
  "mypy>=1.8.0",
  "pytest>=8.0.0",
  "ruff>=0.14.0",
]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.mypy]
python_version = "3.11"
strict = true
warn_unused_configs = true

[tool.deptry]
# Drop-in snippets are transplantable scaffolding and should not affect dependency linting.
extend_exclude = ["^\\.agents/dropins/"]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = [
  "B",
  "E",
  "F",
  "I",
  "UP",
]
ignore = ["UP017"]

