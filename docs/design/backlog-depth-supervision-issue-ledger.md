# Backlog-depth supervision issue ledger

Updated: 2026-07-16 04:17 America/New_York

This is the supervisor-owned ledger for defects and unresolved findings discovered while bringing the automated backlog pipeline to a working baseline. Generated atoms, dossiers, options, plans, and tickets remain system-owned evidence; this ledger records work that the supervising Codex process must ensure is addressed rather than silently losing it between reruns.

## Operating rules

- Every finding receives a stable `BDS-*` identifier, objective impact, evidence, next action, and closure proof.
- `open` and `in_progress` findings must be considered before advancing the affected case or stage.
- A finding is closed only with recorded evidence. A workaround is marked `mitigated`, not `closed`.
- Case-local authoring defects go back to the same author/session while it is making progress. Restart is reserved for clearly nonprogressing, uncorrectable, or effectively fundamental rework.
- The ledger is not a reason to block useful throughput. Findings are prioritized by whether they prevent real evidence, deep research, a causally adequate plan, implementation, or an honest outcome disposition.

## Required invariants

- Use the signed-in host Codex route and subscription provenance; do not fall back to per-token API billing.
- Use `gpt-5.6-terra` while baseline mechanics are being established.
- Use narrow stage-local reruns until each stage works, then perform a minimal full end-to-end proof.
- Do not substitute supervisor-authored research, options, plans, or implementation for system-authored work.
- Do not impose short test timeouts merely because a valid suite is slow.

## Active findings

### BDS-044 - Premerge infrastructure failure overwrote an accepted causal review

- Status: `local_correction_committed_remediation_pr_pending`
- Priority: delivery-blocking, outcome-integrity
- Objective impact: a storage/setup failure before the original-scenario role ran was represented as proof that the implementation did not address the mechanism. This both blocks good throughput and corrupts the causal record with a fabricated critical finding.
- Exact evidence: PR #213 remained open, cleanly mergeable, and green at exact reviewed head `c930a95f6e768ad0674df91a6b61c3c93d55380d`. The retained same-author `report.json` in review run `20260715T212016Z/codex/0` records `approved`, `mechanism_addressed`, `exercised`, and `closed`. `git worktree add` then failed with `No space left on device` before creating a role artifact. The former catch-all path wrote `premerge_original_scenario_failure.json` with `role_artifact_path: null`, replaced the accepted review with `changes_requested`, and added an unsupported finding titled `Original evidence-backed scenario still fails`.
- Root cause: disposable outcome worktrees and retained-oracle lookup were implicitly tied to the controller's `I:` runs tree, and `_cmd_review_merge` classified `OSError`, setup `RuntimeError`, and contract `ValueError` identically to a completed outcome role whose causal predicates failed.
- Required correction: provide explicit absolute host overrides for disposable outcome worktrees and trusted retained-run/oracle assets; classify only `OutcomeRoleDidNotPass` as a causal rejection; preserve accepted review state for setup/transport/contract blocks; and recover legacy catch-all damage from the retained report without another model turn. Recovery must remain bound to the original PR URL and reviewed head, including retained report/review-reference head receipts, so it cannot launder approval onto changed code.
- Follow-up correction and focused proof: moving disposable outcome worktrees to `U:` exposed Git's cross-volume ownership check. Worktree setup first failed with `detected dubious ownership`; after that caller was corrected, runner_core's internal `rev-parse`, `merge-base`, and `status` calls hit the same boundary. Both layers now grant trust only to the exact resolved repository/workspace on each invocation via `git -c safe.directory=<exact-path> ...`; no global Git configuration is modified. The focused original-scenario premerge replay then passed at exact head `c930a95f6e768ad0674df91a6b61c3c93d55380d`: exit 0, both predicates passed, and `execution_integrity` was true. Artifact: `I:\code\usertest_backlog_depth\runs\usertest_implement\_premerge_outcome_roles\03f0d43eb78e28fa\20260715T235525805412Z\original_scenario\outcome_role.json`.
- Merge result: PR #213 merged at `2026-07-16T01:01:41Z` as `2221377fc5ac6e56b99222e81b0485bad2887a03`. The system preserved the accepted review, reran the unchanged original scenario successfully, moved the ticket to `5 - complete`, and honestly retained `unverified` rather than fabricating mitigation after the separate live-role contract defect in BDS-048.
- Closure proof: focused tests cover path overrides, exact command-local Git trust, causal-role failure versus infrastructure block, legacy recovery, mismatch refusal, and idempotency. The exact PR #213 premerge role passed against `c930a95`, and the merge lifecycle preserved the accepted causal review at `2221377`. The local controller correction still requires a committed remediation PR before this systemic finding closes.

### BDS-045 - Historical semantic scratch retention amplified I: usage by over 125 GiB

- Status: `operationally_recovered_retention_control_missing`
- Priority: operational, throughput-degrading
- Objective impact: canonical pipeline evidence was small, but historical `semantic_runtime` trees retained copied environments, caches, and seeded Codex/plugin homes as though they were durable evidence. On I:'s 256-KiB exFAT clusters this consumed enough allocation to block ordinary work even though logical content was much smaller.
- Exact evidence: 20 terminal agent-todoist and 34 terminal agent-orchestrator `semantic_runtime` trees contained 345,123 files, 186,605 subdirectories, and 33,230,231,314 logical bytes. Their conservative I: allocation floor was 169,876,127,744 bytes versus 34,845,724,672 bytes after exact mirrored preservation on U: NTFS. All 54 sources were moved and digest-verified with no copy, permission, path, or removal error. Two orchestrator records still marked `running` were deliberately retained. Host subscription data at `C:\Users\jason\.codex` was not touched.
- Related evidence: five cold usertest cache/temp roots, 35 Open Meal semantic runtimes, `echo_sandbox`, `utmp`, `claude_sandbox`, an old repair backup, a host PR environment, and video output were also preserved under `U:\I-drive-cold-storage`. The durable migration manifest is `U:\I-drive-cold-storage\MIGRATION_MANIFEST_2026-07-15.md`. I: ended with 370.618 GiB free but still reports `Warning / Full Repair Needed`.
- Required correction: make terminal semantic scratch explicitly disposable or relocatable after canonical database/evidence persistence, retain credential-sensitive copies only outside synchronized roots, and distinguish stale nonterminal database state from active execution before cleanup. Do not remove or move host signed-in Codex data. Do not treat this one-time archival recovery as a retention policy.
- Closure proof: subsequent terminal runs keep canonical evidence while semantic scratch is automatically bounded or moved; stale `running` records receive an explicit terminal/state disposition; two consecutive supervised cycles do not recreate material I: allocation growth.

### BDS-046 - Stage-5 fail-first binding produced an outcome oracle that ticket export rejected

- Status: `local_correction_committed_remediation_pr_pending`
- Priority: delivery-blocking, contract-ownership
- Objective impact: a case could complete research, optioning, selection, planning, and ticket readiness but then fail during export, preventing valid actionable evidence from reaching implementation.
- Exact evidence: the full-chain causal-acceptance test reached ticket export and failed with `outcome_role_selected_outcome_contract_unexpected`. The Stage-5 binder added `selected_outcome_contract` directly to a retained ordinary `staged_replay` oracle, while the repository contract permits that prospective contract only on the derived `fail_first_contract` shape without active Stage-3 positive contracts.
- Required correction: preserve the content-addressed Stage-3 oracle as immutable source provenance; derive a distinct planned post-change fail-first oracle with the unchanged verified command and Stage-5-selected success contract; retain baseline positive contracts only as source provenance, not active post-change semantics; keep repository validation fail-closed.
- Current proof: the exact full-chain acceptance reaches research, planning, export, runtime, and outcome validation. The affected suite collected 143 tests: 142 passed and one environment-dependent symlink case skipped. Ruff and `git diff --check` passed. Legacy artifacts without the new provenance envelope remain readable but cannot gain provenance retroactively.
- Closure proof: focused core/repository contract tests cover the derivation and invalid-source boundaries, and the real full-chain fixture reaches successful ticket export without relabeling the historical baseline or weakening validation. The verified local changes still require a focused commit and remediation PR.

### BDS-047 - Broad suites exposed shared fixture drift after causal-contract expansion

- Status: `broad_qualification_green_pr216_ci_corrections_pending`
- Priority: validation-integrity, intervention-cost
- Objective impact: stale test producers obscured real regressions with many duplicate failures and made broad validation appear less stable than the production behavior.
- Exact evidence: five core policy/assembly failures shared one missing outcome-strategy fixture; 35 problem-mining failures shared omitted reads of the new hash-bound origin-evidence index; two positive-path failures plus one qualification failure shared one missing Stage-5 strategy binding; one full-chain fixture used a stale atom snapshot and actionability shape; the research mission was one redundant blank line over its prompt budget. After shared-helper corrections, core passes 100%, problem mining passes 91/91, positive depth passes 9/9, qualification passes, and the mission budget test passes at 99 lines.
- Integrated qualification update: the first complete changed-project backlog partition reached roughly 1,700 checks and exposed one additional stale synthetic review fixture. Its trusted review receipts omitted top-level `reviewed_head_oid`, although production review writers always retain that exact commit and terminal verification correctly requires it independently of immutable ticket identity. The two-field fixture correction passes the exact trusted and untrusted boundary test; only the owning backlog partition is being replayed. This is not a production-gate relaxation or a new pipeline stage.
- Supervisor-interference update: the owning replay later failed only `test_materialized_repair_is_independently_readjudicated_and_records_final_cycle` because dashboard/ledger edits changed the tracked git-status fingerprint while the qualification bundle was sealed. The exact node passed 1/1 in 67.4 seconds once the worktree status remained stable. This was a valid test rejecting a moving supervisor input, not a product regression; the full owning partition must still pass once from a committed, write-frozen tree.
- Final frozen proof: on commit `f46d751b`, the complete backlog partition passed 1,735 tests with one skip and zero failures in 666.9 seconds. No repository write occurred during the partition. This clears the reviewed-head fixture and qualification-transaction boundaries on the exact PR candidate tree.
- Required correction: keep fixture producers on the same public contract constructors as production artifacts where practical; when a contract expands, update shared producers once and preserve intentional negative tests through explicit opt-outs. Count duplicate failures by root cluster rather than as independent defects.
- Closure proof: the affected full files and then the complete non-Docker component suites pass from the committed combined tree; subsequent contract changes fail in shared constructors rather than dozens of downstream tests.

### BDS-048 - Supervisor guidance overruled a correct immutable-outcome-contract rejection

- Status: `case_correction_verified_systemic_gate_committed_pr_pending`
- Priority: outcome-integrity, delivery-quality
- Objective impact: PR #213 could merge production and ordinary regression work while its mandatory live and mitigation commands were impossible to execute. This allowed code/test throughput but prevented the system from proving that the selected solution actually produced the planned real-world outcome.
- Exact merged defect: the completed plan requires `packages/runner_core/tests/test_maintenance_image_cleanup_isolated_daemon.py::test_protected_external_container_and_partial_error_controls` and `...::test_batch_and_direct_capacity_recovery_pairs`. Merge commit `2221377fc5ac6e56b99222e81b0485bad2887a03` instead contained three differently named tests under `apps/usertest_implement/tests`. Neither planned node had ever existed, and none of the fifteen required stdout evidence markers existed in the merged Python tree. The postmerge live role therefore exited before collection with `file or directory not found`, and outcome progression correctly remained `unverified`.
- Supervisor-caused acceptance: the first automated review in `20260715T211224Z/codex/0/review_summary.json` correctly classified the untouched planned path and absent live result as an error. The focused supervisor prompt in `20260715T212016Z/codex/0/prompt.txt` then incorrectly said the stale planned path was non-authoritative, relocation was advisory-only, and missing literal outcome strings were not a defect. The corrected review followed that instruction and approved. Flexible implementation placement and immutable verification executability were wrongly conflated; this was not nondeterministic model noise.
- Additional real mechanics defects: the merged opt-in module parsed `docker port` before publication could be observed and used GNU-only `df -B1 --output=avail` against a BusyBox DIND image. Ordinary CI skipped the module without all five isolated-Docker settings, and implementation verification never ran it.
- Current correction: the exact original implementation author/session `019f6766-c0f5-7253-b2f0-00b03d1c046a` corrected the postmerge branch using Desktop-bundled Codex `0.144.2`, `gpt-5.6-terra`, and the signed-in host ChatGPT subscription. PR #215 merged the exact planned nodes, port-publication polling, BusyBox-compatible fail-fast free-space probe, and real digest-pinned isolated-DIND controls. The unchanged original scenario and the exact live and mitigation roles then passed at verification commit `49d88252`; the case advanced to `mitigated`, not `resolved`.
- Required systemic correction: at the clean exact reviewed head, preflight mandatory original, live, and mitigation commands before merge. For recognized `python -m pytest path::node` commands, require the path and node to exist and retain a side-effect-free collection receipt. A definite missing path or node is a correction-required outcome-contract defect. Unknown command shapes remain reviewable rather than generically rejected. Ordinary alternate test placement remains a scope advisory; only immutable outcome-command executability is hardened.
- Closure proof: both exact planned nodes collect and pass with all five explicit DIND settings and no skip/xfail; the live node emits its three exact markers from asserted observations; the mitigation node emits all twelve true markers from real paired observations; the unchanged original scenario passes; all roles rerun against the correction commit; outcome records `mitigated`, not `resolved`; and a controller regression proves alternate implementation placement may be advisory while a definitely missing mandatory outcome path/node prevents merge.

### BDS-049 - Ordinary review correction and stable-head PR progression required manual supervision

- Status: `local_correction_committed_remediation_pr_pending`
- Priority: throughput, self-healing continuity
- Objective impact: the system could produce a correctable review finding or an approved draft PR, but it did not carry either state forward autonomously. The supervisor had to resume the author, mark the PR ready, wait for CI, and trigger merge even when the reviewed head was unchanged. This added hours of handling without improving research, implementation, or outcome quality.
- Exact evidence: PR #215 required two focused same-author corrections, a manual ready-for-review transition, and manual merge after 80/80 checks passed. The corrections retained author session `019f6766-c0f5-7253-b2f0-00b03d1c046a` and converged, so restarting or discarding them would have been wasteful. The final independent bounded review found zero blockers at head `c46c767b57c3a462094b40c84e7bb6173015fd0a`.
- Local correction: the continuous loop now durably resumes an ordinary generated ticket once after `changes_requested`, preserving the exact author/session and returning to review on the next reconciliation pass. It persists causal acceptance for the exact PR URL and exact reviewed head; an approved draft at that unchanged head can be marked ready without another model review, pending CI is treated as an operational wait, terminal-green and mergeable state uses the existing merge path, and any head change forces a fresh review. IDEA-originated work remains excluded.
- Current proof: the focused continuous-loop bridge has 12 passing tests; the complete review module has 45 passing tests; Ruff and diff checks pass. The current PR still used manual lifecycle actions because these local changes were not yet committed or deployed.
- Closure proof: commit and merge the controller correction, then demonstrate one generated-ticket review correction and one unchanged-head draft/CI/merge progression without supervisor lifecycle action. A changed head must still trigger fresh review.

### BDS-050 - Verification-only correction could not be recorded without corrupting implementation provenance

- Status: `local_correction_committed_remediation_pr_pending`
- Priority: outcome-integrity, current-case blocking
- Objective impact: the original implementation is correctly recorded as PR #213 at merge commit `2221377fc5ac6e56b99222e81b0485bad2887a03`, while PR #215 only makes the already-selected verification contract executable. Existing outcome progression requires role execution at the immutable implementation commit, so it cannot execute the corrected checks at descendant `49d882520b50678ab5b394218cf4ea5868189d93` without either failing or falsely replacing the implementation provenance.
- Exact evidence: Git proves `2221377` is an ancestor of `49d88252`, and `origin/codex/backlog-depth-remediation` points to `49d88252`. The ticket and implementation ledger both retain `merged_commit: 2221377...`, state `unverified`, and the successful original-scenario receipt from that implementation. PR #215 merged after 80/80 checks and contains only the outcome-verification correction required by the immutable plan.
- Required correction: retain implementation PR/commit as immutable provenance; bind one write-once verification-amendment PR/commit only when it is a descendant on the target branch; run roles at that effective verification commit; and retain both implementation and execution provenance in every new receipt. Reject unrelated, ancestor, off-branch, or differently rebound amendments while making exact reapplication idempotent.
- Current proof: the completed ticket and implementation ledger atomically bind amendment `outcome_verification_amendment:2ffe9cae...`, retain PR #213 / `2221377` as implementation provenance, and separately retain PR #215 / `49d88252` as verification provenance. Schema-4 original, live, and mitigation receipts carry both commits and the amendment id. The real progression reached `mitigated` and terminal provenance now verifies with zero errors.
- Closure proof: the binding is atomically identical in the completed ticket and implementation ledger; exact original, live, and mitigation roles execute at `49d88252` while preserving `2221377` as the implementation commit; all resulting receipts verify both values; and the case advances to `mitigated`, never `resolved`.

### BDS-051 - Progress reporting was fragmented across detailed benchmark sections

- Status: `dashboard_committed_current_cycle_active`
- Priority: supervision efficiency, observability
- Objective impact: detailed benchmark and contract evidence existed, but there was no single lifecycle-level reference showing how long each meaningful cycle took, what failed, what the system corrected itself, what required supervisor intervention, and where the active run currently stood. This made it harder to distinguish objective progress from repeated hardening work.
- Correction: the metrics companion now contains a top-level operational dashboard backed by a JSON ledger. It has one row per meaningful pipeline/case lifecycle cycle, explicit timing coverage, furthest/current state, outcome, concrete error categories, automatic correction count, supervisor intervention count, and next action. Unknown historical values remain `unknown`; detailed benchmark data stays below rather than being blended into a promotional score.
- Current proof: 14 lifecycle rows cover the retained baseline through completed PR #215 outcome verification and the active controller-integration cycle; exactly one row is current; four cards report only current state, current disposition, qualification progress, and the next gate. The completed PR #215 row remains immutable. The active row now records 3 h 23 m 25 s of wall time through the latest observation, eight high-level encountered error categories, twenty-six explicit supervisor interventions, zero automatic model-stage corrections for this controller integration cycle, two 40/40 replacement CI runs at exact head `9190f49e`, and the 380.6-second exact-head Terra review. That review accepted approach alignment, mechanism coverage, original-scenario exercise, and scope, while requesting a versioned selected plan and clearer presentation of current versus historical outcome evidence. The dashboard already labels `snapshot`, `current_status`, and `ticket_and_outcome_status` as point-in-time/superseded objects; the current source now makes that boundary explicit and binds the current maintenance truth to mitigated with verified original/live/mitigation/terminal provenance, never resolved. The JSON-backed generator and invariants remain the update boundary.
- Closure proof: the dashboard and its generator are committed, the completed PR #215 row is preserved, and the new active row is updated rather than replaced as qualification, PR, and the later shadow cycle progress. Final closure requires the current row to reach an honest terminal disposition and the next cycle to become current.

### BDS-052 - Outcome lifecycle writes amplified Windows line endings and caused false plan drift

- Status: `local_correction_committed_remediation_pr_pending`
- Priority: current-case blocking, provenance integrity
- Objective impact: the verification-amendment binder correctly requires the completed ticket to remain bound to its immutable exported body and plan, but repeated lifecycle writes changed malformed Windows newline bytes without changing the plan text. The real bind therefore stopped before mutation with `Durable outcome ticket provenance is stale or cross-plan: ticket_body_sha256`.
- Exact evidence: the completed ticket contains 15,355 repeated carriage-return-before-line-feed sequences, currently mostly `0D 0D 0D 0A`. Existing canonical normalization interprets the extra carriage returns as extra blank lines, yielding current body hash `983aa59f...` instead of stored/export hash `05352358...`. Normalizing one-or-more carriage returns immediately before line feed as one newline reproduces the immutable stored body hash `05352358a76a10fb6395ae252b2b476c67fea02f797992ad801cff4713d22f0f` and plan hash `349351ebc6d4afe69369575eee77c7fbc1a93e6727078c04a36bfda0fb90be1f` exactly. Actual content was not changed.
- Root cause: atomic outcome ticket writers decode existing bytes and then use platform text newline translation, adding another carriage return before existing line feeds on Windows. Canonical normalization handled ordinary CRLF and lone CR but treated repeated CR-before-LF as multiple logical newlines.
- Required correction: canonicalize repeated CR-before-LF as one newline, preserve ordinary CRLF/lone-CR semantics, and make atomic ticket writes byte-stable across repeated Windows lifecycle updates. Retain immutable body/plan hash rejection for actual content or cross-plan drift.
- Current proof: focused fixtures preserve the immutable export hashes across repeated Windows lifecycle writes and still reject substantive drift. The real amendment bind succeeded without weakening the stored body or plan hashes. The terminal verifier reopened the historical file with all 15,355 repeated CR-before-LF sequences, recovered the Stage-6 contract, and returned zero errors.
- Closure proof: a completed-ticket fixture with repeated lifecycle writes still matches its export-time hashes; identical amendment binding is byte-preserving; actual plan text drift remains rejected; the real PR #215 amendment binds atomically without weakening stored provenance.

### BDS-053 - Successful outcome progression retained a disproven transient blocker

- Status: `local_correction_committed_remediation_pr_pending`
- Priority: outcome integrity, current-case cleanup
- Objective impact: all three amendment-bound outcome roles passed and the case correctly advanced to `mitigated`, but `remaining_risks` still included `Post-merge outcome verification is blocked: outcome_oracle_asset_missing`. A terminal successful disposition must not retain a runner-owned blocker that the later successful run disproved.
- Exact evidence: the first centralized progression stopped before role execution because the trusted U: retained-runs override was omitted and recorded the transient blocker. The corrected foreground run completed in 984.7 seconds, produced passing schema-4 original, live, and mitigation receipts at execution commit `49d88252`, and left zero provisioner containers. The durable ticket and ledger agree on `state: mitigated`, yet the stale blocker remained alongside the legitimate plan residual that mitigation is not resolution.
- Required correction: after a required role successfully executes, remove prior generic `Post-merge outcome verification is blocked:` risks regardless of whether the setup/transport detail contains that role name. Preserve researched residual risks and all evidence/provenance. Reconcile an already-terminal ticket and ledger atomically without rerunning successful roles or rewriting history.
- Current proof: the completed ticket and ledger are identical at `mitigated`; the stale `outcome_oracle_asset_missing` blocker is absent; the only remaining risk is the researched statement that mitigation does not establish resolution. Evidence counts remain one test, two original-scenario, one live, and one mitigation receipt, and a terminal replay completed with `roles_run=[]`.
- Closure proof: focused tests retain non-generic plan risks while clearing generic transient blockers; the real ticket and ledger remain identical at `mitigated`; the stale asset blocker is absent; all schema-4 role evidence and dual commit provenance remain byte-identical.

### BDS-054 - Newly terminal outcomes bypassed terminal provenance verification

- Status: `local_correction_committed_remediation_pr_pending`
- Priority: outcome integrity, current-case blocking
- Objective impact: `progress_post_merge_outcome` verifies provenance when a record is already terminal on entry, but it returned `complete` immediately after newly transitioning this case to `mitigated` without calling the same verifier. All causal roles passed, yet the terminal provenance contract is not currently verifier-clean, so the dashboard cannot honestly treat the lifecycle as fully complete.
- Exact evidence: a direct verifier call with both trusted roots (`I:\code\usertest_backlog_depth\runs` and `U:\usertest_backlog_component_validation\runs`) reports seven errors: newline-sensitive local-plan lookup; review ref and summary comparing pre-merge provenance to the later `verified_implementation_head` enrichment; schema-2 implementation provenance rejected as though only schema 1 existed; and three missing Stage-6 role contracts caused by failed plan lookup. The underlying artifacts exist and the three schema-4 role receipts bind implementation `2221377`, execution `49d88252`, and amendment `2ffe9cae...`.
- Required correction: use raw newline-safe plan reads; compare immutable ticket provenance fields while validating verified-head enrichment independently; accept and hash-validate the existing schema-2 implementation provenance contract; recover the Stage-6 role contracts; use both the controller runs root and configured trusted retained-runs root; and call terminal verification immediately after a new terminal transition.
- Guardrail: this is not a new PR-scope or perfection gate. It enforces the already-promised outcome/branch/evidence provenance at the exact point where the system claims terminal completion.
- Current proof: 49 focused provenance/progression tests pass, including substantive plan drift, forged reviewed heads, invalid schema-2 receipts, altered Stage-6 contracts, newly-terminal failure, and dual-root propagation. Ruff and diff checks pass. A direct verifier call using the explicit I: and U: roots returned `verified: true`, `provenance_status: verified`, and `errors: []`. A real terminal progression returned `starting_state=final_state=expected_state=mitigated`, `status=complete`, and `roles_run=[]`, so no expensive outcome role was repeated.
- Closure proof: focused negative tests still reject changed plan bytes, forged reviewed heads, invalid schema-2 receipts, altered role contracts, and untrusted paths; the real mitigated record verifies with zero errors using the two explicit roots; and a progression regression cannot return `complete` for a newly terminal record whose provenance fails.

### BDS-055 - Secondary postprocessing overwrote a precise missing-artifact failure

- Status: `broad_qualification_green_remediation_pr_pending`
- Priority: validation integrity, self-healing feedback quality
- Objective impact: a selected implementation attempt could pass its broker verification but lose a required retained artifact. The runner initially detected that exact evidence-integrity failure, then replaced it with `binary_or_command_missing`. Incorrect causal feedback can send the same author or supervisor toward Codex installation/authentication instead of repairing the actual missing artifact.
- Exact evidence: the broad delivery/runtime partition passed 927 checks with four skips and failed this one node. The isolated node reproduced in 6.19 seconds. Its artifacts show agent exit 0, broker source `broker_reuse`, successful verification, matched workspace state, and no missing broker response artifacts. The fake agent deliberately removed only `raw_events.attempt1.jsonl`. `error_traceback.txt` shows all-attempt normalization reopened that missing path after final materialization had already written `selected_attempt_artifacts_incomplete`; the outer catch then overwrote `error.json`, and the generic text classifier mislabeled `No such file or directory` as a missing binary.
- Root cause: all-attempt event normalization introduced after the selected-artifact fail-closed contract used the attempt path even after materialization had recorded it missing and created a canonical placeholder. The outer exception path treated any later postprocessing exception as the primary terminal cause.
- Local correction: after a known selected-artifact materialization failure, normalize the canonical placeholder rather than reopening absent attempt paths. If later postprocessing still raises, retain the existing specific `error.json` and write the secondary exception to `postprocess_error.json`. Host `CODEX_HOME`, signed-in `.codex` data, host-login enforcement, API-key clearing, and subscription provenance are unchanged.
- Current proof: the corrected missing-artifact path and an injected secondary-normalization failure both retain `selected_attempt_artifacts_incomplete`; the latter writes the secondary artifact without changing the primary cause. Both parametrized cases plus healthy broker-reuse and healthy multi-attempt aggregation controls pass 4/4 in 55.7 seconds.
- Closure proof: on frozen commit `f46d751b`, the complete delivery/runtime partition passed 929 tests with four skips and zero failures in 1,157.4 seconds. The remediation PR must preserve both specific failure classification and successful multi-attempt normalization.

### BDS-056 - Qualification supervision lost sibling results and mutated sealed inputs

- Status: `operational_correction_verified_current_cycle`
- Priority: supervision efficiency, validation integrity
- Objective impact: useful broad-suite work was discarded or invalidated even though the underlying system was making progress. This increases intervention cost and can create false product failures, directly opposing the throughput objective.
- Exact evidence: a direct `Promise.all` rejected when the backlog partition failed, discarded the already-exited CLI summary, and severed the still-running delivery process from its result channel. The delivery process finished after about 19 minutes but produced no usable receipt, requiring one standalone replay. During the backlog replay, concurrent dashboard/ledger edits changed the sealed `git status --porcelain` hash; the exact transaction test passed on stable bytes.
- Required operational correction: use independent result cells or all-settled capture with a durable per-partition result artifact; never rely on one combined fail-fast payload. Freeze and preferably commit the exact tree before git-status-sensitive qualification, and make no repository writes until each sealed partition exits. Treat lost or supervisor-invalidated results as unknown, never as pass or product failure.
- Current proof: the lost delivery result was not counted. The final backlog, delivery/runtime, and CLI/tools partitions ran in independent cells on frozen `f46d751b` and retained exact results: 1,735/1, 929/4, and 141/3 passed/skipped respectively, all with zero failures. No repository write occurred while they ran, and no result process was orphaned. The two exact isolated-Docker tests then passed in 954.4 seconds with zero provisioner DIND containers before and after.
- Closure proof: the current cycle demonstrates the corrected operating method. The future automated qualification runner should encode independent durable result capture and a sealed-tree lease rather than depend on supervisor discipline; until then this remains an operationally verified manual control, not autonomous closure.

### BDS-057 - Local qualification omitted isolated changed-project boundaries before PR publication

- Status: `replacement_ci_green_next_pr_control_pending`
- Priority: delivery efficiency, validation integrity
- Objective impact: frozen component qualification was correctly green for the causal pipeline behavior, but it did not reproduce every changed project's isolated CI command. Three branch-owned defects therefore reached the first PR matrix, adding a correction cycle after publication and delaying the first shadow run without improving research or solution quality.
- Exact evidence: draft PR #216 at `e94b6ce8e217fa75aa04295ffa7c0488e78b0bc0` triggered push run `29478853671` and pull-request run `29478913132`. Each completed 40 jobs with 37 passes and the same three failures; the duplicate triggers are three unique failures, not six. `backlog_miner` failed Ruff `I001` on one import block in `test_full_chain_causal_acceptance.py`. `runner_core` failed only `test_failed_legacy_sandbox_expectation_can_be_revalidated_without_mutation` because the Windows-only failed-receipt premise is intentionally false on POSIX project-execpolicy. `usertest_backlog` failed only `test_materialized_repair_is_independently_readjudicated_and_records_final_cycle` because branch commit `ce248229` replaced valid sibling-test imports with a monorepo-root `apps.*` path that isolated package execution does not expose.
- Root cause: final local qualification used broad repository-source partitions and selected-file Ruff rather than the exact changed-project scaffold commands. That masked the app test's accidental repository-root import, did not exercise the Windows-specific fixture under Linux semantics, and omitted the newly changed backlog_miner file from lint. This is one CI-equivalence coverage gap with three concrete manifestations, not three new pipeline-control categories.
- Correction: Ruff organized the backlog_miner import without semantic change; the legacy sandbox-revalidation test is now explicitly skipped outside native Windows rather than weakening POSIX or Windows production verification; and the app transaction test restores its prior package-local sibling imports without adding `sys.path`, repository-root `PYTHONPATH`, or a cross-project `tests` package. Signed-in host Codex data, host-login enforcement, API-key clearing, subscription provenance, and production execution policy are unchanged.
- Current proof: package-wide Ruff passes for `backlog_miner`, `runner_core`, and `usertest_backlog`; the exact Windows runner test passes; the exact CI-path `usertest_backlog` project suite passes in 334.2 seconds; and the previously run full-chain acceptance test passes after the import ordering correction. A combined long-running local verification channel was interrupted and its result was not counted; the authoritative owning-boundary reruns are retained instead.
- Replacement-CI proof: corrected head `9190f49e6f483ace34f706c1e43645d5bdd5b397` passed push run `29480914169` and pull-request run `29480916329`, each 40/40. These are the same 40 unique checks under duplicate triggers, not 80 independent checks. The immediate correction is verified; full closure still requires the next PR candidate to execute its exact changed-project boundaries before publication rather than learning the same class from CI.
- Required operational action: before first push, derive every changed project and run its configured package lint/test boundary, while retaining narrower tests for fast iteration and broader causal partitions for cross-project confidence. Capture each long result independently so one tool interruption cannot discard completed siblings. Do not restart the full backlog pipeline for a package-local validation defect.
- Closure proof: replacement push and pull-request CI at the corrected head pass all project and smoke jobs. The next PR candidate runs exact changed-project boundaries before publication and does not repeat this class of post-publication correction.

### BDS-004 — Progressing research is prematurely restarted by a cost heuristic

- Status: `closed`
- Priority: objective-blocking
- Objective impact: destroys good throughput and prevents self-healing even when the same researcher has materially improved its result.
- Evidence: `stage3_maintenance_supervised_boundary_20260714T112500Z` ended as `restart:correction_cost_reached_investigation_cost`. Attempt 30 moved to `research_status=evidence_sufficient`, `actionability_assessment.disposition=requires_change`, zero blockers, and zero material unknowns, but had correctable verifier findings.
- Local correction: persisted `evidence_verification_feedback` is now restored as an external-review frontier; resolved prior findings are evaluated before the cost cutoff; and a safe transformed-error candidate becomes the forward correction baseline without displacing the objective best. A candidate that retains old findings and adds more still rolls back.
- Verification: nine focused tests and the full `test_research_runner.py` suite pass (101 tests in 65.94 seconds).
- Recurrence evidence: `stage3_maintenance_attempt36_docker_recovered_20260714T194200Z` again stopped as `restart:correction_cost_reached_investigation_cost` after attempt 45 crossed 603.289 correction seconds against a 468.664-second original investigation. Attempt 45 had nevertheless reworked four missing atom bindings into four concrete bindings that the verifier could reject more precisely as `not_bound_to_observation`. The runner compared attempt 45 only with the six-error objective-best frontier and ignored attempt 44's immediate 12-error feedback frontier, so it recorded no resolved errors and let cost alone stop the same author. Attempts 43-45 and their workspace changes remain retained; none is accepted for optioning.
- Required systemic correction: measure immediate-feedback progress separately from objective-best progress. A safe equal-count replacement continues without resetting the cost clock; a lower immediate-feedback count may reset it. Cost is a secondary signal and may stop only repeated observed nonprogress or fundamental rework, never the first changed/no-op correction by itself. Keep the six-error objective best unchanged until a genuinely better dossier is verified.
- Current local correction: the adaptive repair loop now retains an immediate-feedback frontier separately from the safe forward and objective-best frontiers. Equal-count identity rework continues without resetting cost, a smaller immediate-feedback set resets the next-turn cost clock without automatic promotion, and a cost restart requires at least two consecutive genuine-nonprogress turns. Consecutive changing transport failures still consume the same bounded economics.
- Current verification: five focused policy/integration regressions pass, and the complete `test_research_runner.py` module passed 107/107 in 62.8 seconds before the later churn-safeguard additions; the current superset passes 111/111 in 86.7 seconds.
- Live verification result: the same Terra author did continue instead of being immediately restarted and produced 11 retained correction runs under `stage3_maintenance_same_author_oracle_repair_20260714T204137Z`. That proves the immediate-feedback/cost fix restored author continuity, but it exposed the separate substantive-frontier defect in BDS-018: changing or smaller verifier sets could reset progress indefinitely while stronger causal content disappeared. The orphaned outer process was stopped at 17:54 after the loop met the explicit nonprogress threshold. Its exact Python/Docker-exec descendants are gone, Docker has zero containers, and no artifact, image, or workspace was removed.
- Closure proof: met. Focused policy tests pass and the same maintenance researcher received its verifier findings and produced 11 retained next attempts instead of a premature cost restart. The newly exposed substantive-churn behavior is tracked separately as BDS-018 rather than keeping the corrected cost-policy defect open.

### BDS-018 - Mechanical verifier churn can erase substantive research and run indefinitely

- Status: `reclassified_under_BDS-019`
- Priority: objective-blocking, throughput-degrading
- Objective impact: a correction loop can look mechanically active while moving away from the actual objective. It may reduce or replace validation findings by deleting an independently supported root-cause facet or executable success contract, and changing error identities can then keep the same loop alive without producing a decision-ready dossier.
- Evidence: the first two retained runs in `stage3_maintenance_same_author_oracle_repair_20260714T204137Z` preserved two root-cause hypotheses, including cleanup-before-build recovery, and an explicit positive outcome contract. Later candidates reduced the mechanical frontier to one or two findings but retained only the age/count hypothesis and dropped the operational positive contract. Runs from `20260714T210300Z` through `20260714T214308Z` cycled among `research_falsification_intervention_invalid`, `primary_falsification_survival_missing`, a seven-error causal cascade, and a two-error falsification pair. Eleven complete candidates were retained; none restored the stronger causal coverage. A twelfth replay had begun when supervision paused it.
- Required correction: retain three distinct notions: immediate-feedback progress, mechanical objective best, and substantive coverage. Fewer verifier findings count as improvement only when established, independently evidenced causal coverage and executable outcome coverage are not silently removed. Unsupported substantive loss should quarantine the candidate and return precise restore feedback to the same author. After a small consecutive sequence with no mechanical-objective or substantive advancement, return a resumable `repairable_paused` disposition with the strongest frontier and all candidates retained; do not restart or discard the investigation.
- Guardrail against over-hardening: this is not a requirement for perfect dossiers or fixed benchmark-specific fields. Evidence-backed epistemic downgrades remain valid, one or two imperfect corrections remain repairable, and any non-regressing lower-error candidate resets progress normally. The pause exists to hand control back for better feedback, not to block useful throughput.
- Local correction: the runner now projects general substantive proof roles (supported/mechanism/falsification hypothesis coverage, origin-atom experimental coverage, controlled production touchpoints, and executable outcome coverage) independently of raw error count. Unsupported loss is quarantined against the safe frontier and receives exact restoration feedback; three consecutive losses pause with retained work. Equal-count error-identity churn remains forward work for two corrections and pauses on the third. A lower error count, deeper verifier frontier, restored/added substantive coverage, or evidence-backed epistemic revision resets the applicable churn state.
- Verification: the complete `test_research_runner.py` module now passes 123/123 in 126.7 seconds with a one-hour ceiling and external temp storage; the six resume-helper tests, the three controlled-call identity checks, Ruff, `py_compile`, and scoped `git diff --check` also pass. The suite proves unsupported-downgrade, substantive-loss, lateral-churn, and ordinary-nonadvancing state is retained rather than restarted or discarded.
- Corrected accounting: the numbered supervision ledger through record 48 is not a count of model turns. It contains 40 completed model reports, seven verifier-only feedback snapshots (records 3, 7, 11, 14, 20, 23, and 29), and one model-free recovery record (46). A filesystem audit found 62 completed `gpt-5.6-terra` reports for this same maintenance case, totaling 10,885.2 seconds of recorded agent wall time, plus the interrupted run labeled attempt 49. The old 46/48-attempt narrative understated the actual retry volume and mixed unlike record types.
- Controller-failure evidence: numbered attempt 19 was verifier-clean and causally sufficient. Forty-seven of the 62 completed model reports, consuming 8,219.07 seconds (about 76% of the total), occurred after that point. The earliest defensible controller stop was numbered attempt 11; even under a conservative standard the absolute latest defensible stop was attempt 20. Continuing through attempt 48 and starting attempt 49 was therefore not justified research iteration. It was a controller and stage-contract failure, tracked as BDS-019.
- Current correction: the counter now recovers only from hash-valid, same-session retained sibling attempts bound to the exact safe source and error frontier, carries through independent-feedback transitions, and pauses after the general three-turn threshold without restarting or deleting work. Authenticated prior feedback and the latest supervisor note are now foregrounded before the full contract; an unsupported downgrade requires newly executed, retained, dossier-cited counterevidence. This is a correction-economics control, not a universal tool-count or quality gate.
- Current disposition: no additional same-author Stage 3 retry is warranted. The run labeled attempt 49 was interrupted, its partial artifacts were retained, and its exact orphan processes were stopped. No artifact, Docker image, container, cache, workspace, or run directory was deleted or moved. A real Stage 4 handoff already produced one count-only option, which Stage 5 rejected before planning. The next proof is immutable persisted-evidence authentication followed by same-session Stage 4 revision using the exact retained Stage 5 feedback.
- Closure proof: focused and full runner tests still cover candidate quarantine and repair-state persistence, but BDS-018 is no longer the governing explanation for this case. BDS-019 must prove that a causally sufficient dossier exits Stage 3 instead of entering further corrective turns.

### BDS-019 - Controller continued Stage 3 after causal sufficiency

- Status: `closed_for_current_case_monitoring_generalization`
- Priority: objective-blocking, throughput-destroying
- Objective impact: the pipeline spent most of this case's model time trying to satisfy misplaced future-solution proof requirements after the root mechanism was already established. One real Stage 4 handoff produced a shallow option that Stage 5 correctly rejected, but the controller then routed those solution-design gaps back into Stage 3. Accepted-option and plan throughput remained zero, and a working researcher looked like repeated research failure.
- Record-type audit: the legacy sequence numbered 1-48 comprises 40 model reports, seven verifier-only snapshots, and one model-free recovery record. Those numbers must never again be reported as 48 model attempts.
- Filesystem audit: 62 completed same-case `gpt-5.6-terra` reports total 10,885.2 seconds of recorded wall time, plus an interrupted 63rd model run labeled attempt 49. Of the completed reports, 40 are directly numbered, 11 belong to an orphan continuation chain, and 11 are unnumbered side/adjudicated runs.
- Sufficiency point: numbered attempt 19 is verifier-clean, records `research_status=evidence_sufficient`, has zero verification errors, binds all three source atoms across six experiments, and establishes the production count-or-age mechanism with the controlled 49-fresh versus 3-aged result. It is the canonical causal Stage 3 artifact.
- Stop-policy assessment: attempt 11 was the earliest defensible stop because the causal direction and production locus were already established. Attempt 19 was fully verifier-clean. Attempt 20 is the absolute latest defensible stop under any reasonable allowance for confirmation. The 47 completed model reports after attempt 19 consumed 8,219.07 seconds, approximately 76% of the total measured model time, without creating a legitimate reason to remain in Stage 3.
- Root cause: Stage 3 readiness required a verified post-change operational outcome contract whose exact solution behavior properly belongs to optioning, falsification, planning, and outcome verification. That circular boundary made research prove a future implementation before Stage 4 was allowed to design it. The controller then treated downstream design work as missing research and kept issuing correction turns.
- Required correction: Stage 3 must end when current evidence establishes a real unactioned case, a source-bound causal mechanism, the production locus, counterevidence or deterministic causal closure, actionability, relation disposition, and no root-material unknown. Stage 4 must define candidate success properties and safety constraints; Stage 5 must adversarially review their causal adequacy; Stage 6 must bind them to exact executable before/after checks; the outcome stage must supply actual post-change proof. Preserve stronger legacy research outcome evidence when present, but do not require future-solution proof to leave Stage 3.
- Local correction: the Stage 3 qualification-repair adapter now advances from the controller's current authenticated payload instead of reloading the older dossier collection on every correction. An authenticated ready payload with valid persisted evidence short-circuits additional research; cost remains telemetry, and the general three-turn material-nonprogress rule retains the strongest frontier rather than restarting it.
- Verification: all 23 qualification-repair runtime/materialization tests plus three focused current-payload regressions pass. The retained attempt-19 dossier advanced through one Stage 4 option, one Stage 5 selection, and one Stage 6 plan without another Stage 3 model invocation. The final assembled chain remains post-change unverified; this proof closes the current case's stage-exit defect, not its implementation outcome or generalization to unseen cases.
- Closure proof: met for the current case. Attempt 19 passes corrected Stage 3 readiness and persisted-evidence authentication without mutation; downstream design findings stayed with Stages 4-6; and the exact chain reached `ready_for_ticket`. Preserve the 62-report, 10,885.2-second overrun as historical controller-failure evidence and monitor the stop behavior on future nondeterministic cases.

### BDS-020 - Plain Windows Codex resolution selected an obsolete npm CLI after restart

- Status: `reopened_stale_installed_runtime_bypassed_resolver`
- Priority: baseline-execution blocking
- Objective impact: every otherwise valid local backlog stage could fail before producing work because `configs/agents.yaml` intentionally configures the portable name `codex`, but Windows `PATH` selected an older npm installation that did not support the requested `gpt-5.6-terra` model. Repeating or repairing model output could not correct this host-launch mismatch.
- Exact evidence: after the 2026-07-14 host restart, `where.exe codex` selected `C:\Users\jason\AppData\Roaming\npm\codex` first and that command reported `codex-cli 0.139.0`. The current Codex Desktop-managed host executable under `%LOCALAPPDATA%\OpenAI\Codex\bin\<opaque-install-id>\codex.exe` reported `codex-cli 0.144.2`. No installation hash or user profile path is encoded in the correction.
- Root cause: local execution, login, command-availability preflight, and CLI-version preflight did not share a Desktop-aware resolver. Ordinary `shutil.which("codex")` therefore allowed the stale npm shim to outrank the current Desktop-managed CLI after environment reconstruction on restart.
- Local correction: one exported resolver now preserves every explicit configured path; for only the plain Windows names `codex` and `codex.exe`, it discovers structurally valid Desktop-managed candidates below `%LOCALAPPDATA%`, orders them by recency, requires a successful local `--version` probe, and otherwise falls back to ordinary `PATH`. The adapter's login and execution paths and the runner's local availability/version preflights use that same decision. Docker-prefixed commands are unchanged and continue to resolve inside the container.
- Subscription boundary: the correction changes only the executable. It retains the canonical host `CODEX_HOME`, exact `Logged in using ChatGPT` requirement, canonical ChatGPT routing overrides, and child-only blanking of `OPENAI_API_KEY`, `CODEX_API_KEY`, `CODEX_ACCESS_TOKEN`, `OPENAI_BASE_URL`, `OPENAI_API_BASE`, `OPENAI_ORG_ID`, and `OPENAI_ORGANIZATION`. It neither copies nor removes host Codex data and introduces no API-key fallback.
- Verification: focused resolver, execution, preflight, and subscription-auth tests pass. The complete affected adapter/backlog-auth/provenance/preflight/exec-policy suite passes with the 24-hour test ceiling. A live non-model probe resolves plain `codex` to the current Desktop-managed executable, records `codex-cli 0.144.2` in the runner version receipt, and reports exact ChatGPT login with all blocked API/provider environment variables blank. No model, Docker operation, cleanup, deletion, or move was performed for this repair.
- Closure proof: met for the baseline blocker. A subsequent real stage invocation still owns model-level throughput proof, but it no longer needs a pinned installation hash or supervisor-selected executable path to reach the supported host Codex CLI.
- Recurrence on controller review: standalone system-review runs `20260716T075924Z/codex/216` and `20260716T080245Z/codex/216` stopped before reviewer dispatch. Their ordinary PowerShell backend probes passed with `shell_probe=ok`, but the installed `apps/usertest/.venv` copy of `agent_adapters` predated the Desktop-aware resolver and selected npm Codex `0.139.0`. Terra returned HTTP 400 requiring a newer CLI; the runner then incorrectly collapsed that model/CLI error into `shell_probe_failed` and advised the already-selected `inspect` policy. Allowlisting `LOCALAPPDATA` did not change the local agent environment. A process-local `PATH` prefix selected the Desktop-managed `0.144.2` binary without installing or editing host software; a live Terra probe and the 380.6-second review then completed through exact ChatGPT login with API/provider variables blank.
- Required follow-up: runtime/package freshness must be part of the entrypoint contract, and agent-level provider/model incompatibility must retain its actual diagnostic rather than masquerading as shell-policy failure. Do not block the current causally reviewed PR merely because the bootstrap controller used a stale venv; prove the next workspace-current invocation selects the Desktop resolver without supervisor `PATH` repair.

### BDS-058 - The controller remediation plan was absent from the reviewed commit

- Status: `local_correction_ready_same_reviewer_pending`
- Priority: review-blocking, provenance integrity
- Objective impact: the system review could trace production behavior and tests but could not independently reconstruct the selected intervention from the exact reviewed commit because the 79,865-byte execution plan lived only under the ignored `.agents` tree. This is directly relevant to the objective that deep research and planning—not post-hoc PR prose—drive implementation.
- Exact evidence: review run `runs/usertest/usertest_backlog_depth/20260716T080509Z/codex/216` is bound to head `9190f49e`, used `gpt-5.6-terra`, and returned `approach_alignment=aligned`, `mechanism_assessment=mechanism_addressed`, and `original_scenario_oracle=exercised`, but `changes_requested` because `Test-Path` for `.agents/plans/3 - in_progress/20260709_historical_automated_backlog_depth_remediation.md` was false in the clean reviewed checkout. The ignored local source plan has SHA-256 `D7C942A28BF1E404637E6A16F4FEC4454A2AF5E6502A1D49C8497E7572FEE3EA`.
- Correction: force-add the exact existing execution plan at the documented path without changing its bytes. This preserves the original detailed decisions and evidence rather than replacing them with a shorter retrospective summary. Return the new exact head and content hash to reviewer session `019f69f5-bedd-7b00-9941-09502f89765a`; do not start a fresh review author.
- Closure proof: the plan blob is present in the pushed reviewed commit with the exact pre-commit SHA-256, replacement CI is green, and the same reviewer accepts or narrows its finding against the new exact head.

### BDS-021 - Single-path causal work was rejected as unsupported shared-abstraction breadth

- Status: `closed_for_current_case_monitoring_generalization`
- Priority: objective-blocking, throughput-degrading
- Objective impact: Stage 5 asked the option author to remove pre-resolution cleanup and caller-specific protection because the option had only one runner-evidenced scope path. That would preserve the surface-level failure this remediation is intended to prevent: a locally correct cleanup predicate placed after the real operation had already exhausted storage.
- Exact evidence: the first selector in `stage4_maintenance_lifecycle_rework_20260715T020353Z` returned `option_revision_requested` and said to separate the cleanup-helper change from resolver/manual extensions because `scope_evidence.scope_level=single_path`. Repository inspection instead shows `run_batch_preflight` executes a storage-writing `docker buildx build --load` before calling `resolve_maintenance_docker_image`; resolver-only cleanup cannot recover the batch after that earlier failure. The option did not claim a new class-wide abstraction: it extended an existing cleanup helper and its existing callers along one reachable maintenance operation.
- Root cause: the two-independent-path guard for new shared abstractions and class-wide claims was applied to the number of files/callers touched by one causal path. The selector therefore optimized for narrow PR surface rather than complete root-cause coverage.
- Local correction: generic and internal Stage 4/5 prompts and guidance now reserve the two-path rule for broad problem claims and newly introduced reusable abstractions. A single evidenced operation may cross existing components, extend an existing helper, and require sequencing/protection changes. Selector/falsifier guidance now requires locating the earliest same-resource failure boundary, preserving safe useful throughput after partial auxiliary failures, and avoiding benchmark/default values as unsupported universal maxima.
- Verification: 81 relevant planning, Stage-5 self-healing, policy-boundary, and causal-scope tests passed with the 24-hour ceiling; Ruff and diff checks passed. In the real supervised rerun, the exact original selector session corrected its critique, identified the pre-scratch-build boundary, partial-cleanup throughput rule, configured-count invariant, full current-identity protection, and post-cleanup inventory requirement. That corrected feedback was delivered to the exact original option-author session, which produced a materially revised option.
- Closure proof: met for the current case. Continue monitoring sealed/general cases for false breadth rejection without weakening the genuine two-independent-path gate for new shared/class-wide claims.

### BDS-022 - Optional Stage 3 baseline contract is treated as the exclusive future-solution gate

- Status: `closed_for_current_case_monitoring_generalization`
- Priority: objective-blocking, systemic
- Objective impact: an otherwise strong option cannot reach planning because Stage 5 requires an immutable pre-change research contract to prove the future implementation's end-to-end recovery. Stage 4 cannot repair that contract, so same-author correction loops on an impossible request or downgrades valid work.
- Exact evidence: in `stage5_maintenance_lifecycle_same_selector_20260715T022239Z`, the original option author revised the solution to run bounded identity cleanup before the batch scratch build and direct resolver writes, verify actual Docker inventory, preserve the complete current identity, and require successful scratch-build-plus-resolution continuation. The selector provisionally accepted it. The falsifier ultimately rejected only because retained `positive_outcome_contract:5bcbc293...` proves the Stage 3 fresh-burst identity-count baseline and cannot already observe the future pre-scratch-build implementation. The option's `causal_coverage.outcome_strategy` already names every demanded future observation. A second option revision made that strategy even more explicit, but the loaded validator still routed the immutable Stage 3 contract back to the option author.
- Root cause: `bind_falsification_review` chooses mutually exclusive paths: it reviews the Stage 4 outcome strategy only when research has no positive contracts; when any legacy research contract exists, it requires that contract to be sufficient post-change proof. This conflates current-mechanism evidence with prospective solution semantics and contradicts the Stage 3/4/5/6 boundary.
- Required correction: always review and content-address the selected option's Stage 4 outcome strategy. Treat retained Stage 3 contracts as optional baseline/additional evidence. A surface-only research baseline must not block selection when the Stage 4 strategy would prove intended operation; a surface-only Stage 4 strategy must still block. Stage 6 must convert the accepted strategy into exact executable checks, and implementation/outcome execution must supply the actual post-change proof.
- Supervision disposition: the live run was stopped without deleting artifacts when it began a second falsifier cycle under the uncorrectable loaded contract. The strongest same-author revised option and selector acceptance are retained and will be used as the next Stage 5 input after the systemic boundary is fixed; Stage 3 and Stage 4 will not be restarted.
- Closure proof: focused and affected suites pass; Stage 5 accepts the retained strong option while accurately labeling the research baseline contract as partial/surface-only; the selected Stage 5 outcome contract is content-addressed from the sufficient Stage 4 strategy; and Stage 6 receives that strategy for exact post-change verification.
- Live closure evidence: `stage5_maintenance_outcome_boundary_replay_20260715T025512Z` selected `option:pre-resolution-bounded-identity-cleanup-with-contextual-protection`. The independent falsifier accepted the prospective Stage 4 outcome strategy as sufficient with full problem coverage after three same-session mechanical corrections. The bound record is `outcome_contract_status=approved_for_planning` and `post_change_evidence_status=unverified`; all six model/auth manifests verify the signed-in host ChatGPT subscription route with API fallback disabled. This advances the case to Stage 6 without claiming implementation or resolution.

### BDS-023 - Terminating the shell wrapper did not terminate the live stage descendant tree

- Status: `open`
- Priority: operational, supervision-integrity
- Objective impact: a supervisor can believe a pathological correction loop is stopped while the Python controller and Codex descendants continue consuming subscription capacity and writing new artifacts. Metrics and handoff state then drift after the declared cutoff.
- Exact evidence: the wrapper cell for `stage5_maintenance_lifecycle_same_selector_20260715T022239Z` reported `Script terminated`, but the original `run_stages456_one.py` Python process (PID 45316), its Python 3.13 child (PID 21180), and a later Terra Codex child continued. The orphaned controller completed a third option revision at 02:42:31Z and started another selector/falsifier cycle. At 02:42:58Z the live tree was identified by exact output-root command line and explicitly stopped (Codex PID 16592, Python PIDs 21180 and 45316). No artifact was deleted or rewritten.
- Required action: stage cancellation must terminate and await the complete process tree, then persist a cancellation receipt containing descendant PIDs, final artifact frontier, and whether any post-request writes occurred. Metrics must use the actual final filesystem frontier, not the earlier wrapper cutoff.
- Current disposition: include the post-cutoff third author revision and subsequent partial cycle as orphaned retained evidence; do not count that orphaned cycle as completed. A separate clean replay, `stage5_maintenance_outcome_boundary_replay_20260715T025512Z`, started from the strongest authenticated option revision and completed Stage 5. BDS-023 remains open only for general process-tree cancellation correctness.
- Closure proof: a focused long-running child fixture is cancelled; all descendants exit; no later artifact timestamps appear; and the cancellation receipt matches the observed process tree.

### BDS-024 - Selected retention outcome is bounded above but does not forbid over-deletion

- Status: `planning_contract_closed_implementation_proof_pending`
- Priority: solution-correctness, implementation-blocking
- Objective impact: an implementation that deletes every ordinary unprotected maintenance image could satisfy the selected `ordinary identities <= keep_local_count` property and recover capacity while violating the intended retention behavior. That would replace the original accumulation failure with destructive over-cleaning.
- Exact evidence: the selected Stage 5 contract in `stage5_maintenance_outcome_boundary_replay_20260715T025512Z` requires ordinary unprotected identities to remain at or below `cfg.keep_local_count`, actual post-cleanup Docker reclamation, and successful constrained continuation. It does not explicitly require retaining the newest `min(cfg.keep_local_count, eligible ordinary identity count)` identities. Its `original_scenario_experiment_ids` contains only `experiment:pytest-fresh-source-bound`; the authenticated `experiment:pytest-initial-single-hash` observation is not carried as a below-budget compatibility scenario.
- Disposition: do not restart Stage 3 or discard the accepted option. Give the Stage 6 planner this exact gap and require an executable compatibility check that a below-budget inventory is unchanged plus a multi-identity assertion that the newest in-budget ordinary identities remain while only overflow identities are removed. The plan must carry this alongside the existing capacity-recovery, current/protected identity, external-tag/container blocker, and partial-error controls.
- Current proof: the final Stage 6 plan at `stage6_maintenance_image_decision_complete_20260715T123600Z/change_plans.json` requires a below-budget unchanged replay and newest-first retention of exactly `min(keep_local_count, eligible ordinary identity count)` during overflow. The independent semantic audit found no remaining over-deletion loophole.
- Closure proof: the planning half is met. Implementation and post-change outcome execution must still pass both checks before this problem may be called mitigated in operation.

### BDS-025 - Stage 6 requires future post-change live proof before it may plan that proof

- Status: `closed_for_current_case_monitoring_generalization`
- Priority: objective-blocking, systemic
- Objective impact: a causally sufficient case and adversarially approved outcome strategy are returned to research because the future implementation's exact live command has not already run before implementation. This makes a decision-complete plan impossible and drives the same kind of zero-throughput loop that kept Stage 3 open after attempt 19.
- Exact evidence: `stage6_maintenance_image_stage5_final_terra_20260715T032500Z` received the verifier-clean Stage 3 dossier and the final Stage 5 selection `option:pre-resolution-bounded-identity-cleanup-with-contextual-protection`, inspected the repository, and then emitted zero plans with `planning_status=research_required`. Its stated blocker was the absence of a runner-verified live Docker experiment for the future capacity-recovery behavior. The planning prompt simultaneously says Stage 6 owns exact after-change commands and predicates grounded in Stage 3 baseline evidence, Stage 5 semantics, and inspected source, but later requires every live command to bind an exact research experiment that already ran that command. Those requirements are circular for a new solution.
- Required correction: retain the authenticated Stage 3 before-state replay and Stage 5 outcome contract. Permit Stage 6 to define a new exact post-change live command, predicates, repository touchpoints, and harness integration from those inputs while keeping its execution state explicitly `unverified`. Require pre-existing research binding for the original/before scenario and for any claim about current behavior; require implementation/outcome execution before the new live command can support `tests_verified`, `live_verified`, or `resolved`. Return to research only when root cause, interface choice, change surface, or required outcome semantics remain materially undecided.
- Current execution boundary: continue the same planner session `019f63d1-90ba-7b41-bf2c-4e9b76d3ad0b` with this exact feedback after the systemic contract is fixed. Do not rerun Stages 3-5 and do not replace the author merely because its first answer followed contradictory instructions.
- Current proof: the same planner session produced plan revision `planrev:sha256:ed4d010e810deab419cffd90b5804ca892f19dd48b4570ea1be2ed657975c725`. Its future isolated-daemon checks are explicitly `planned_unverified`, the maximum outcome is `mitigated`, and neither research nor selection was rerun. Planning and ticket-readiness validation pass without treating the unexecuted future command as existing evidence.
- Closure proof: met for the current case. Keep the distinction under monitoring on future plans; implementation and live execution remain separate gates.

### BDS-026 - Stage 6 pauses on cumulative correction cost after material progress

- Status: `closed_for_current_case_monitoring_generalization`
- Priority: throughput-blocking, systemic
- Objective impact: the planner can reduce a large mechanical error set to a small repairable frontier, then be paused because accumulated correction time exceeds the first response cost. This treats elapsed effort as stronger evidence than objective progress and strands good authored work instead of correcting it.
- Exact evidence: in `stage6_maintenance_image_same_planner_continuation_20260715T033500Z`, exact planner session `019f63d1-90ba-7b41-bf2c-4e9b76d3ad0b` reduced validator findings from 37 to 8 to 2. The two-error response is `implementation_planning_001_correction_002.response.txt`, SHA-256 `3744d8c2f5edc844630f2ea258c45075a4b15b800857a92930af916ccd89e4a6`. Two later replies regressed, after which the controller returned `repairable_paused:correction_cost_reached_original` because 700.14 correction seconds exceeded the 258.90-second initial response. The best frontier and same author remain available; there is no evidence that its two remaining issues are uncorrectable or equivalent to restarting.
- Required correction: preserve the objective-best valid-item/error frontier independently of the latest response and continue the same author from that frontier when later turns regress. Error-count reduction or valid-item increase is progress regardless of error identity. Cumulative cost alone must not pause a materially improved, low-error frontier. A pause remains appropriate only after repeated non-advancing work from the same best frontier, an uncorrectable external condition, or rework demonstrably equivalent to restart.
- Current execution boundary: do not start a new planner or rerun Stages 3-5. Authenticate the exact two-error response, classify those two findings, and return only legitimate corrections plus any substantive independent depth finding to the same session.
- Current proof: elapsed correction cost is now telemetry rather than a stop condition. The exact planner session continued from the retained frontier through the mechanically green four-turn run and then received the independent semantic findings. It corrected those findings in two additional authenticated turns rather than being restarted or discarded. The final two-turn correction used about 301 seconds end to end (290.03 seconds of recorded model-turn time).
- Closure proof: met for the current case and focused controller regressions. Continue monitoring nondeterministic cases for genuine three-turn material nonprogress rather than reintroducing an elapsed-time cutoff.

### BDS-027 - Mechanically valid Stage 6 plan leaves core interfaces and live proof architecture undecided

- Status: `closed_at_planning_gate_implementation_review_pending`
- Priority: solution-depth, implementation-blocking
- Objective impact: a plan can pass structural readiness while leaving the implementer to design the cross-module API, cleanup-result semantics, and live capacity harness. That would recreate the historical failure mode where detailed prose masks unresolved implementation and proof decisions.
- Exact evidence: planner attempt 4 (`implementation_planning_001_correction_003.response.txt`, SHA-256 `a189f4b519f04d35c7279b49955bb7f56413aebd069c7b5224c58a617eea2bde`) is mechanically ready after correcting the pytest `-p no:cacheprovider` parser false positive. It correctly addresses the count-or-age root cause and BDS-024, but independent repository-grounded review found three material gaps. First, it does not name the prepared-identity type/function, exact fields, resolver handoff parameter, cleanup protection input, or cleanup-result/artifact schema; the batch target omits the existing `run_batch_preflight` symbol, and removal of `keep_local_days` does not name `MaintenanceDockerConfig` or `_load_maintenance_docker_config`. Second, the new isolated-daemon test does not specify daemon provisioning, storage-capacity control, subprocess routing, host-state isolation, or baseline/recovery inventory equivalence, and its predicates do not prove baseline failure, blocker preservation, safe partial state, failed-continuation blocking, or manual-cleanup behavior. Third, it reuses the aged Stage 3 causal control as a recurrence probe even though recurrence belongs to the centralized two-shadow-cycle check and age grace is intentionally removed.
- Disposition: preserve the exact same planner session, mechanism, targets, before/after scenario, and non-over-deletion requirements. Return one focused Stage 6 correction that chooses exact APIs/data flow and status fields; makes the live harness and paired predicates executable; leaves recurrence empty/centralized; and clarifies whether the `.usertest_research` replay is repository material or outcome-runner materialization. This is planning work, not a return to Stage 3.
- Current proof: the final plan names the prepared-identity owner and fields, every cleanup phase, ordered operation attempts and exception-path persistence, exact batch and resolver control points, server-owned replay staging, the reachable loopback isolated-daemon architecture, arm-qualified paired predicates, and centralized recurrence. The content-addressed independent audit `stage6_maintenance_decision_complete_semantic_audit_20260715.json` records all three prior material findings resolved, zero remaining material defects, and `ready_for_supervised_implementation`.
- Closure proof: met at the planning gate. The audit retains two nonblocking implementation-review checks—failed-attempt evidence must remain reachable after exceptions, and each isolated pair must assert its own daemon endpoint and alias survival. Those are implementation conformance checks, not reasons to reopen research or planning.

### BDS-028 - Immutable Stage 5 causal coverage is regenerated as planner prose

- Status: `closed`
- Priority: throughput-blocking, systemic
- Objective impact: Stage 6 must carry the selected option's causal-coverage contract unchanged, but the pipeline asks a nondeterministic planner to reproduce the entire object and then reports only a generic mismatch. Semantically identical copy drift therefore consumes correction turns without improving the plan.
- Exact evidence: in `stage6_maintenance_image_depth_correction_20260715T131500Z`, the objective-best response `implementation_planning_001_correction_004.response.txt` differs from the Stage 5 selected option at exactly `$.causal_coverage.residual_recurrence_paths[4]`: Stage 5 says `outside this maintenance-image retention case`; Stage 6 says `outside the maintenance-image retention case`. All other causal-coverage fields are identical. The validator returned only `change_plan_causal_coverage_linkage_mismatch`; corrections 5 and 6 retained that same sole error, and correction 6 was byte-identical to correction 5.
- Required correction: bind immutable selected-option causal coverage and scope evidence server-side when finalizing a Stage 6 candidate, preserving the exact Stage 5 objects and their provenance. If any remaining immutable linkage is model-authored, validator feedback must identify the exact differing JSON pointer and expected value rather than a generic mismatch.
- Current proof: Stage 6 finalization deep-copies the exact selected Stage 5 `causal_coverage` and `scope_evidence` objects. The current final plan binds selection SHA-256 `f616ff3987922fd7a9c2a0323db213aeb4914ae209f1db0ac068bdda5a272aef` and passes the affected validator suite without asking the planner to reproduce immutable prose.
- Closure proof: met. Materially different upstream contracts remain rejected; equivalent prose drift no longer consumes a correction turn.

### BDS-029 - Stage 6 requires a plan-authored recurrence command for centralized recurrence

- Status: `closed`
- Priority: objective-correctness, throughput-blocking
- Objective impact: recurrence is owned by the pipeline's two-shadow-cycle case refresh, but Stage 6 validation requires nonempty recurrence commands and predicates. The planner is forced to misclassify a Stage 3 causal replay as operational recurrence, undermining the outcome model and generating avoidable corrections.
- Exact evidence: the bound Stage 6 feedback explicitly required empty plan-authored recurrence commands/predicates and centralized ownership. The planner's correction 2 followed that boundary and received `missing_outcome_role_commands:recurrence`. Later corrections added the below-budget research replay solely to satisfy the validator. That replay verifies compatibility at the recorded revision; it does not establish absence of recurrence across future shadow cycles.
- Required correction: represent centralized recurrence with an explicit machine-verifiable ownership marker and allow empty plan-authored commands/predicates only for that ownership mode. Continue to require and validate commands/predicates when recurrence is genuinely owned by the plan.
- Current proof: the final plan uses `verification_owner=centralized_case_refresh` with empty recurrence commands and predicates, explicitly assigning recurrence to two later shadow-cycle refreshes. Plan-owned recurrence remains command-validated in focused negative tests.
- Closure proof: met.

### BDS-030 - Stage 4 selected an old-behavior assertion as the post-change original-scenario replay

- Status: `closed_for_current_case`
- Priority: outcome-correctness, implementation-blocking
- Objective impact: a plan can claim an exact original-scenario binding while replacing the retained test asset that defined that experiment. The resulting after-change command is no longer the authenticated research experiment, and outcome materialization can collide with a repository-created file at the same destination.
- Exact evidence: the selected Stage 4/5 outcome strategy names `experiment:pytest-fresh-source-bound`. Its command runs `.usertest_research/test_maintenance_image_burst_replay.py::test_fresh_burst_retains_source_symptom`, exits 0 before the change, and asserts that all 49 identities remain. The Stage 6 plan then proposes creating/replacing that same path with post-change assertions while retaining the research binding. `runner_core.outcome_roles` rejects an existing retained-asset destination as `outcome_oracle_materialization_collision`. The same Stage 3 dossier already contains `experiment:fresh-burst-contract-fail-first`, whose unchanged command exits 1 before the fix because it asserts the intended bounded behavior and is designed to pass after the change.
- Required correction: distinguish authenticated before-state observations from commands that are safe to replay unchanged as post-change proof. Prefer a verified fail-first or explicitly post-change-replay-eligible experiment when available. When none exists, permit an honest `planned_unverified` alternate integrated through inspected repository boundaries without pretending it is the exact research command; do not create a new circular Stage 3 execution requirement for the future solution.
- Current execution boundary: correct the option through the same optioner session, then rerun the same selector/falsifier against the revised immutable option contract before returning to Stage 6. Do not restart Stage 3.
- Current proof: one authenticated Stage 4 correction in optioner session `019f6384-3b6b-7503-983a-71f8e3dbdfa7` replaced the old-behavior selector with `experiment:fresh-burst-contract-fail-first`. Stage 5 reselected and falsified that exact contract. Stage 6 now uses the unchanged content-addressed retained asset, requires exit 1 with retained count 49 before implementation and exit 0 with retained count 5 afterward, and introduces no `.usertest_research` change target.
- Closure proof: met for selection and planning. The unchanged after-state replay has now passed after each of six system-authored implementation commits, with retained count 5 at configured bound 5. Live Docker, recurrence, and durable OutcomeRecord evidence remain absent.

### BDS-031 - Falsifier prompt and validator required different evidence and residual-risk shapes

- Status: `closed`
- Priority: throughput-degrading, self-healing correctness
- Objective impact: a substantively useful falsifier could be rejected for following the written contract, then spend correction turns rediscovering a schema rule instead of testing the proposed mechanism.
- Exact evidence: the validator required each material-risk disposition to use `evidence_refs` that match cited review refs and required every `outcome_strategy_review.residual_untested_paths` string to appear verbatim as a disposed risk. The prompt instead described `mechanism_evidence_id` refs and did not state the exact-string residual binding. The resulting findings were formatting/contract mismatches rather than evidence that the selected mechanism was shallow.
- Local correction: generic and internal falsifier prompts now require `evidence_refs`, explicitly reject `mechanism_evidence_ids` as a substitute, and require verbatim residual-risk text. The direct root diagnostic is returned to the same falsifier rather than being hidden behind downstream findings.
- Closure proof: the current Stage 5 falsifier session corrected its own output under the aligned contract and reached `accept`; deterministic selection/falsification validation is clean.

### BDS-032 - Stage 5 recovery helper created its invocation tracker after the model turn

- Status: `closed_for_retained_recovery_helper_monitor_production_path`
- Priority: operational, throughput-degrading
- Objective impact: a valid completed labeler turn appeared to fail at wrapper level because the throw-away recovery helper could not attest an invocation it began tracking only afterward. Re-running the model would waste a correct authored result.
- Exact evidence: the labeler under `stage5_maintenance_revised_replay_reassessment_20260715T013000Z` completed one authenticated response, but the helper failed during post-turn materialization. The retained receipt now labels it `accepted_recovered_after_wrapper_interruption`, preserving session `019f657d-b8ad-7630-866c-2ae3ef50508a` and response SHA-256 `816f1ff5fa5d6edba8c0cf671a9b21f2ae5ff902cbea3e94605ea20f33e4950c`.
- Local correction: recovery materialization now reuses and verifies the exact retained selector, falsifier, and labeler invocation/auth manifests instead of rerunning a completed role or manufacturing a post-hoc tracker history. Stage 5 materialized one selection with status `ready_for_stage6`, selection SHA-256 `f616ff3987922fd7a9c2a0323db213aeb4914ae209f1db0ac068bdda5a272aef`, and API fallback disabled.
- Closure proof: met for this retained helper. A normal production Stage 5 run still needs monitoring so a wrapper interruption cannot be confused with model failure.

### BDS-033 - Stage 6 ticket readiness used a hand-authored Stage 2 priority fixture

- Status: `current_exact_chain_verified_narrow_materializer_correction_pending`
- Priority: evidence-lineage, readiness-integrity
- Objective impact: a real Stage 3-6 plan could be labeled `ready_for_ticket` while the prioritization link in the assembled ticket was synthetic. That proves planning mechanics, not an exact authenticated full chain.
- Exact evidence: `.tmp/component_validation/run_stage6_one.py` supplied a hard-coded priority object to `assess_ticket_readiness`. The first mechanically green Stage 6 wrapper therefore did not prove current Stage 2 provenance. An independent raw-artifact audit also correctly found that the narrow Stage 2 stage artifact omits `case_id`; production `staged.py` attaches canonical lineage at the orchestration boundary, so this is not yet classified as a core Stage 2 producer defect.
- Current correction and proof: a real `gpt-5.6-terra` prioritizer produced `stage2_maintenance_image_current_terra_20260715T144500Z/prioritized_problems.json`, SHA-256 `a58a767718c0b2f5b789d0c8b65a0a09d7f9f5910f3dcc12d5e75246b092d814`, through the signed-in host ChatGPT subscription with API fallback disabled. Exact assembly through production `propagate_case_lineage` with the current Stage 3, Stage 4, Stage 5, and Stage 6 artifacts returns one ticket, `stage=ready_for_ticket`, and ticket readiness `true / []`. The narrow materializer is being corrected model-free to perform that same orchestration step.
- Closure proof: the exact current chain is sufficient for supervised implementation once the model-free materializer preserves production lineage. A synthetic priority object must never again count toward full-chain readiness.

### BDS-034 - Structural Stage 6 readiness accepted a semantically incomplete plan

- Status: `closed_for_current_case_adversarial_gate_monitoring_generalization`
- Priority: solution-depth, readiness-integrity
- Objective impact: the first four-turn Stage 6 result passed deterministic plan and ticket checks yet still allowed an implementer to omit pre-write control points, invent or overbuild phase/attempt telemetry, or satisfy aggregate recovery predicates with only one of the two selected operation arms. Calling that result implementation-ready would recreate the surface-level planning failure this project is meant to stop.
- Exact evidence: `stage6_maintenance_image_final_same_planner_20260715T124500Z` ended mechanically green after four authenticated same-planner turns. Independent review then found exactly three material defects: incomplete authorization of batch and resolver pre-write controls; undefined or overbuilt phase/attempt ownership and exception-path state; and aggregate recovery predicates that did not independently require batch and direct-resolver baseline/recovery proof.
- Self-healing correction: those three content-addressed findings went back to planner session `019f63d1-90ba-7b41-bf2c-4e9b76d3ad0b`. It corrected them in two authenticated turns at `stage6_maintenance_image_decision_complete_20260715T123600Z`, about 301 seconds end to end, without restarting Stages 3-5. The final plan SHA-256 is `51c821cd9cf685d7f7347386878a62c360c8af94c717d51266b6ccbcabdafe13`.
- Closure proof: independent receipt `stage6_maintenance_decision_complete_semantic_audit_20260715.json`, SHA-256 `1e4aa5c5487a9e9652270eb72fd3708d333bd0459b8b0d34215c13a5f77731a2`, binds the exact Stage 3/5/6 hashes, records all three findings resolved, finds zero new material shallow-fix or proof loopholes, and returns `ready_for_supervised_implementation`. This is one real case, not a general semantic-precision rate; keep an adversarial review step while measuring future false-ready frequency.

### BDS-035 - UTF-8 BOM made an existing Python target symbol appear missing

- Status: `closed`
- Priority: mechanics, throughput-degrading
- Objective impact: the planner could name the correct `run_batch_preflight` symbol and still receive a false missing-symbol error, consuming same-author corrections unrelated to solution quality.
- Exact evidence: the clean revision's `apps/usertest_implement/src/usertest_implement/batch_preflight.py` begins with U+FEFF. `_python_target_bindings` passed those bytes directly to `ast.parse`, caught the resulting syntax error, and returned no bindings. The Stage 6 validator then reported the real symbol as unresolved.
- Local correction: symbol grounding strips one leading UTF-8 BOM before parsing while still rejecting genuine syntax errors. The focused BOM-positive and syntax-error-negative cases are included in 69 passing symbol/planning tests.
- Closure proof: the corrected validator resolves the existing batch-preflight symbol, and the final plan binds it successfully. This changes only source parsing; it does not relax target existence or symbol checks.

### BDS-036 - Ticket exporter and runtime outcome-role validation reject the server-bound fail-first replay accepted by Stage 6

- Status: `closed`
- Priority: implementation/export blocking, contract-integrity
- Objective impact: the exact plan passes Stage 6 and ticket readiness but cannot enter the normal ticket/implementation path. Treating that as a model or planning failure would rerun correct authored work; bypassing the exporter would hide a real integration break.
- Exact evidence: the production exporter was invoked against the current exact assembled maintenance chain and failed before any write with `outcome_role_selected_positive_contract_ids_invalid`. The plan's original-scenario role is a server-bound `staged_replay` with `scenario_kind=fail_first_contract` and the selected Stage 5 outcome contract. It correctly has no research `positive_outcome_contracts`. Stage 6/ticket readiness accepts that current contract, while the exporter and runtime `outcome_roles` path still require research positive-contract IDs for non-`stage5_planned` replay.
- Root cause: server/export/runtime validators do not share one acceptance rule for the revised staged fail-first contract. The downstream paths still encode the superseded assumption that every non-planned replay must be backed by a research positive outcome contract.
- Correction: exporter and runtime validation now recognize a fail-first replay only when `scenario_kind=fail_first_contract` is paired with its valid bound, approved/unverified Stage 5 selected outcome contract. Ordinary staged, causal, and multi-scenario replay roles still require their research positive-contract coverage. The Stage 5 contract ID and repository revision now remain available to the live and mitigation bindings for this server-bound form.
- Focused validation: six exact fail-first-positive and ordinary/malformed adversarial pytest cases passed, and Ruff passed. This is focused contract-parity evidence, not implementation or outcome proof.
- Closure proof: the unchanged exact chain exported one implementation ticket with fingerprint `03f0d43eb78e28fa`. Export `maintenance_image.tickets_export.json` has SHA-256 `423e8af26029e122306e0d7fab46471419f5bb08a94b31e9840307b15c8a4aa6`; materialization receipt SHA-256 is `1d49220de6e346f255e1cd79920d003fa33f5b8fecf3ad72c748750203829393`. The additive local plan has raw-file SHA-256 `dfe83820fc6c95dd227ea8ca9959b0dbc56c2a16151e861e7423a7cdc012bab8` and canonical local-plan SHA-256 `349351ebc6d4afe69369575eee77c7fbc1a93e6727078c04a36bfda0fb90be1f`; its verification and target contracts are SHA-256 `f3e3a62c1a2bd426ed81745e2dccc6c3414f1503ed2a2c24b8cc764112cb5c98` and `98ff1d11ffeea4f537cde3cd9b89cd5d6b393fbe6a9c61ea345cbe0f599278fb`. Exact target revision remains `51e35fbcb50151590604611f10910a7fd13660e4`. No existing ticket was moved or removed, no model stage was rerun, and no Docker action occurred.

### BDS-037 - Retained original-scenario asset is not transported into implementation verification

- Status: `closed`
- Priority: implementation blocking, verification-integrity
- Objective impact: the exported ticket carries the correct unchanged fail-first command, but a fresh implementation workspace cannot run that command unless the content-addressed `.usertest_research` asset it names is also transported. Starting a paid implementation turn without that deterministic prerequisite would waste model work and could turn a valid plan into a false implementation failure.
- Exact evidence: the supervised `usertest implement` dry-run selected fingerprint `03f0d43eb78e28fa` at exact ref `51e35fbcb50151590604611f10910a7fd13660e4`, but deterministic pre-model validation proved that `.usertest_research/test_maintenance_image_burst_replay.py` is absent from the fresh target checkout. The retained source asset is under the external research root `U:\usertest_backlog_component_validation\runs`; the current implementation contract transports command strings, not the retained file needed by the command. No paid model turn was started.
- Correction: `runner_core.retained_oracle_assets` now transports general content-addressed retained bundles rather than special-casing this maintenance test. The runner validates the asset source and manifest before dispatch, stages it under `.usertest_research`, revalidates immediately before every model turn, excludes it from Git changes, and preserves the exact asset/session/workspace on resume. Missing or mismatched assets fail deterministically before author execution.
- Current proof: asset `outcome_asset:7eef9999c1251ad5ea9db839d215db8ab5929307c86197facb23f4e0133cd147`, manifest SHA-256 `476fe3857442058b97be20038aa8e87f6bc484501ab608a50eee629278243fef`, and transport SHA-256 `0a25b5f0af32a31689d830d5da2f623fdc2a406169fdc208759e0cb7d2fe6821` bind 18 entries. The unchanged replay ran from the retained implementation workspace and passed throughout all eight commits. Unrelated tickets do not inherit the bundle. The final same-author run is `20260715T171250Z/codex/0`; the later model-free PR-adoption receipt recaptured the exact ticket replay unchanged at head `e3f988b8`.
- Closure proof: met for implementation asset transport. Draft PR #213 and its exact plan-command adoption receipt now exist. The later live-Docker and durable OutcomeRecord boundaries remain separate and unproven.

### BDS-038 - One malformed shell rejection was misclassified as a read-only workspace

- Status: `mitigated_for_current_case_monitoring`
- Priority: throughput-degrading, self-healing correctness
- Objective impact: a correct implementation can produce zero code when the author treats a command-policy or quoting rejection as proof that the filesystem is read-only, then repeats the false premise instead of attempting a simple tracked edit.
- Exact evidence: the original implementation run `usertest/20260715T143437Z/codex/0` contains three exit-zero turns in session `019f6635-03a2-7f60-acab-3d97e277332f`, each reporting the workspace read-only and making no code change. Its recorded Codex argv and preflight explicitly say `sandbox=workspace-write` and policy allowed; the rejected PowerShell command is malformed/quote-corrupted, a simpler read succeeds, and no tracked-file write had been attempted. The same retained workspace/session later produced eight commits.
- Current correction: supervisor feedback returned the exact evidence to the same author and required one simple read plus a real tracked `apply_patch` attempt before any read-only claim. Good work was retained; the author was not restarted.
- Required systemic correction: classify filesystem capability from the effective sandbox contract plus an exact simple write attempt, not from one complex command rejection. Persist the exact rejection category and avoid repeating a disproven premise.
- Closure proof: the current case is recovered. A later independent implementation run must self-correct this condition without supervisor intervention before the finding can close generally.

### BDS-039 - Implemented-local correction tried to check out a branch that did not yet exist

- Status: `closed`
- Priority: implementation-blocking, self-healing continuity
- Objective impact: a passing local implementation cannot receive semantic feedback in its exact workspace/session if resume first attempts to check out a recorded branch name that has not yet been created, forcing an unnecessary restart or discarding correct work.
- Exact evidence: setup run `20260715T145047Z` stopped before a model turn with `pathspec 'backlog/03f0d43eb78e' did not match`. The retained workspace and exact Codex session were still present and authoritative.
- Correction: implemented-local semantic resume now requires prior passing verification, the exact retained workspace, and the exact author session; it reuses that workspace without a fresh fallback or premature checkout. Sixteen focused tests passed.
- Closure proof: the same author/session continued through the final `20260715T171250Z` run and produced eight commits on `backlog/03f0d43eb78e`, ending clean at `e3f988b8e391ad020e746d3bc3937fca53cf6363`. No author restart or fresh-workspace fallback occurred.

### BDS-040 - Successful subscription-backed attempts are mislabeled `provider_auth`

- Status: `open_nonblocking_metadata_integrity`
- Priority: observability, nonblocking for current case
- Objective impact: five successful implementation/correction attempts appear to be provider-auth failures, which can corrupt aggregate reliability metrics or trigger unnecessary reruns even though authoritative execution and verification succeeded.
- Exact evidence: five commit-bearing attempt records have `failure_subtype=provider_auth` while `exit_code=0`, runner verification is `passed`, and each target/exec-policy receipt binds the canonical signed-in host ChatGPT subscription route with API billing variables disabled. The final correction run no longer carries the false subtype.
- Required action: fix the failure-subtype classifier and add regression coverage proving an exit-zero, verification-passed, subscription-verified attempt cannot be labeled provider-auth failure. Do not rerun or block the maintenance ticket for this telemetry defect.
- Closure proof: historical records remain auditable as metadata anomalies, new successful runs emit no false subtype, and aggregate auth-failure metrics exclude these five records.

### BDS-041 - Focused implementation verification missed package-boundary and metadata regressions

- Status: `closed_for_current_case_systemic_test_policy_pending`
- Priority: throughput and verification-integrity
- Objective impact: repeatedly green focused commands allowed two maintenance-specific defects to reach PR CI: a cross-package integration test collected under a package that did not install its dependency, and a top-level cleanup metadata assertion inconsistent with the intended prewrite/post-resolution contract.
- Exact evidence: the first PR head failed `runner_core` collection with `ModuleNotFoundError` because the isolated-DIND test imported `usertest_implement` from `packages/runner_core/tests`. After relocation, the full `runner_core` project exposed `test_prepare_maintenance_profile_cleanup_failure_is_best_effort`, which expected the top-level prewrite error while the implementation had overwritten it with the nested post-resolution error.
- Correction: the same implementation author relocated the integration test without weakening its scenarios, then separated top-level prewrite and nested post-resolution metadata. The final same-author attempt also removed inherited `USERTEST_PYTHON` from two probe tests exposed by the complete suite.
- Closure proof: final run `20260715T171250Z/codex/0` passed the unchanged replay, 435 `runner_core` tests with one skip, 10 affected application tests, and collected three isolated-DIND tests without executing them. PR-event CI run `29437882891` passes the `runner_core` project/smoke and maintenance-image jobs at `e3f988b8`; its only three failed project jobs are independently red on base run `29433871787`.
- Required systemic follow-through: before first push, verification selection must include every changed package's collection/full project suite when affordable; focused checks remain useful for fast iteration but are not sufficient as the final affected-package gate.

### BDS-042 - Existing implementation PR lacked a durable non-destructive workflow binding

- Status: `closed`
- Priority: lifecycle integrity, outcome provenance
- Objective impact: branch and PR existence were externally true, but the source ticket still pointed to `implemented_local`; manual ledger editing would lose exact plan-command evidence or falsely imply that the reconciliation command committed, pushed, or created the PR.
- Correction: `usertest-implement handoff adopt-pr` validates the ticket/case/plan, clean branch and head, ancestry, repository, open PR head/base before and after verification, and either reuses exact verification or recaptures the exact Stage-6 command list model-free. It writes explicit existing-head, PR-adoption, verification, resume-state, and ledger receipts while leaving the ticket bytes and bucket unchanged.
- Focused validation: 49 non-Docker tests, Ruff, compilation, CLI help, and a real-input read-only preflight passed. The first real invocation correctly rejected local remote `origin`; rerunning with hosted remote `github` succeeded.
- Closure proof: adoption run `U:\usertest_backlog_component_validation\runs\usertest_implement\_handoff_adoptions\03f0d43eb78e28fa\20260715T180528368226Z` recaptured both exact plan commands at head `e3f988b8` with 86,400-second allowance, no model, and no result reuse. Its ticket resume state and ledger both say `awaiting_review`; the ticket remains byte-identical in `2 - ready`. No push, PR creation, ticket movement, merge, or OutcomeRecord was performed by adoption.

### BDS-043 - Pre-existing base-branch CI failures obscure implementation quality and block delivery

- Status: `pr_open_ci_running`
- Priority: delivery-blocking, cross-platform correctness
- Objective impact: maintenance PR #213 cannot reach the required green-CI and merge boundary even though its changed `runner_core` project, maintenance validation, and causal regression pass. Reassigning these failures to the maintenance author would waste work and contaminate the case correction with unrelated defects.
- Exact evidence: base run `29433871787` at `450a7332` and head push run `29447794892` at `82f73b30` fail the same three project jobs and six tests. The two `agent_adapters` failures come from a generic fixture using Windows `type` on Ubuntu and an explicit Windows PowerShell trace whose relative backslashes were resolved with the normalizer host's POSIX path semantics. The three `backlog_miner` failures execute hard-coded `powershell.exe` on Ubuntu instead of the installed `pwsh` capability. The `usertest_backlog` failure loads sibling first-party packages from `.venv/site-packages` because selected-project test setup exposes only the selected app's source tree; the exact-source qualification seal correctly rejects those imports. A separate Windows `runner_core` PR job passed all 27 smoke tests and failed only GitHub post-action cleanup while the same-head push job passed, so that occurrence is an infrastructure anomaly rather than a product failure.
- Correction: prerequisite PR #214 (`codex/ci-baseline-portability` -> `codex/backlog-depth-remediation`) keeps task selection narrow while exposing the complete monorepo source graph to tests, preserves selected-project lint behavior and package-smoke isolation, makes only recognized PowerShell `Get-Content` paths host-independent, and selects real `pwsh`/`powershell.exe` capability for replay tests. CI fails explicitly if neither PowerShell executable exists; the replay executor does not substitute an unavailable runtime. The qualification exact-file seal remains unchanged and its stale-module negative test still rejects the injected module.
- Local proof: `agent_adapters/tests/test_basic.py` passes 18/18; `backlog_miner/tests/test_research_evidence.py` passes 107 with one expected skip; `usertest_backlog/tests/test_qualification_transaction.py` passes 15/15; the scaffold prerequisite suite passes five with one expected skip; focused qualification guards pass 3/3; Ruff and `git diff --check` pass. No Docker command was invoked.
- Closure proof: PR #214 receives independent review, all GitHub project and smoke jobs pass, and it merges into the remediation branch. PR #213 must then incorporate that exact base, rerun its complete CI matrix, and remain causally accepted before this issue is closed.

### BDS-005 — Edited research artifacts leave stale evidence receipts

- Status: `verification_pending`
- Priority: objective-blocking
- Objective impact: valid iterative research cannot self-heal because improving a throwaway replay makes the retained proof unverifiable unless the pipeline reruns and reseals it.
- Evidence: the same Stage 3 run reported `research_workspace_overlay_changed`, four `research_artifact_changed` findings, and six `research_experiment_receipt_changed:*:command_authorization` findings after the researcher improved `.usertest_research` replay files.
- Current duplicate-artifact evidence: the canonical dossier at `stage3_maintenance_image_final_terra_20260714T005030Z` verifies `(True, [])`, while the later copied dossier at `stage3_maintenance_image_baseline_reverify_20260714T102100Z` fails current persisted-evidence verification with changed mechanism evidence, mechanism provenance/hash, outcome oracles, verification boundaries, and hypothesis receipts. Downstream work now authenticates the canonical dossier and uses the later clean checkout only as a same-revision planning workspace; the stale copy is retained as integrity evidence and is not normalized or deleted.
- Required action: make continuation rerun/reseal changed authorized research artifacts and experiments, or deliver precise feedback to the same author when automatic resealing is unsafe. Never retain stale receipts as if current.
- Closure proof: persisted-evidence verification passes against the corrected dossier and current isolated research workspace without supervisor-authored evidence.

### BDS-006 — Cross-sandbox pytest temp ACL failures are misclassified as research blockers

- Status: `open`
- Priority: throughput-degrading
- Objective impact: an environment artifact can incorrectly turn established mechanism evidence into `insufficient_evidence`.
- Evidence: focused repository tests failed at setup with `PermissionError` for `pytest-of-jason` and an agent-created `C:\Temp\usertest_stage3_maintenance_image`, while direct Python replays of the same production cleanup completed and produced the expected causal pair.
- Required action: provide a per-invocation writable pytest base temp owned by the executing sandbox identity, or classify this exact setup failure as an operational execution problem and retry safely rather than a material research unknown.
- Closure proof: the focused test runs in a fresh continuation without ACL failure, and no dossier lists the temp failure as a material unknown.

### BDS-007 — Runner experiment output references become unresolved after continuation

- Status: `verification_pending`
- Priority: objective-blocking
- Objective impact: the final dossier cannot satisfy artifact coverage even when experiments executed.
- Evidence: final readiness reported unresolved `runner:replay:experiment:*:stdout` and `:stderr` refs for all six retained experiments plus `research_evidence_verification_artifact_coverage_mismatch`.
- Local correction: model-owned projection now strips runner refs from both top-level and nested experiment refs. Unverified fallback preserves the complete prior runner enrichment when model claims are unchanged and explicitly invalidates a stale receipt when claims changed.
- Verification: the full Stage 3 runner suite passes. A model-free replay against the actual failed run retains all 25 top-level artifacts and reduces unresolved runner refs and artifact-coverage findings to zero.
- Closure proof: every runner artifact ref resolves and artifact coverage verification passes.

### BDS-011 — Maintenance Stage 3 causal proof is verifier-clean

- Status: `closed`
- Priority: case-blocking
- Objective impact: Stage 4 must not option a plan until the proof object connects the origin evidence, production mechanism, causal control, and material causal boundary coherently. It must not require the researcher to finish future solution design first.
- Evidence: same-author attempts 31–40 reduced the retained frontier from 14 findings to six while preserving `evidence_sufficient`, `requires_change`, zero blockers, and zero material unknowns. In the latest interrupted continuation, the six retained findings were secondary to `proof_adapter_unavailable:pytest_controlled_difference.v1`. The registered `structured_replay.v1` adapter can read the retained pytest JSON records with `event_json`; switching to direct scripts is not valid because the falsification verifier cannot bind their scalar fresh/aged argument through the helper to the production call. The researcher could not see this direct diagnostic and oscillated between those representations before downgrading established research.
- Current evidence: attempt 44 authored and executed the requested `structured_replay.v1` fresh/aged pair and a separate parameterized fail-first outcome experiment. The exact production cleanup retained 49 one-day identities and three otherwise-equivalent eight-day identities, so the causal result is substantive. It did not authenticate because BDS-017 treated the controlled age argument as a different consumer identity, causing `proof_adapter_harness_dependency_unverified` and the same six downstream mechanism/falsification findings. Attempt 45 made narrower atom-binding errors but no research-file changes and must not supersede attempt 44's harness work.
- Current retained frontier: forensic comparison of all 11 completed runs in `stage3_maintenance_same_author_oracle_repair_20260714T204137Z` identifies `20260714T204256Z/codex/4187152644` as the strongest complete system-authored dossier (report SHA-256 `a8369241...`, canonical dossier SHA-256 `a5390e90...`). It preserves seven experiments, both causal hypotheses, the explicit fail-first positive outcome contract, and both active-reference correction-risk experiments, while reducing the mechanical output to two findings. The following `20260714T204956Z` candidate contributed a real successful-build recovery control but must not replace the full baseline because it dropped both active-reference experiments and produced three findings. All later candidates removed the recovery hypothesis and explicit outcome contract; their smaller mechanical counts do not supersede the stronger frontier.
- Corrected current evidence: numbered attempt 19, retained at `stage3_maintenance_image_final_terra_20260714T005030Z`, is verifier-clean and causally sufficient. Its exact production replay retains 49 one-day identities and three otherwise-equivalent eight-day identities, covers all three source atoms with six experiments, records zero verifier errors, and substantively establishes the count-or-age mechanism. The later two-error record was stronger in some prospective solution detail but was not required to establish the causal Stage 3 result.
- Disposition: no more researcher correction is required for causal binding. The 62-report retry history is controller waste after a sufficient result, not evidence that this mechanism remained unproved.
- Closure proof: met by attempt 19's verifier-clean, source-bound causal proof. Downstream stages remain responsible for choosing a safe mechanism, defining adequate success properties, planning exact checks, and proving the implemented outcome.

### BDS-012 — Maintenance optioning previously selected a shallow/unsafe mechanism

- Status: `open`
- Priority: next-stage blocking
- Objective impact: the earlier option removed age grace and proposed count-only deletion without proving active/protected/running/shared-reference safety, so it did not adequately solve the problem.
- Evidence: real run `stages456_maintenance_image_live_terra_20260714T103000Z` handed the case to Stage 4. Session `019f6103-fd2c-7120-b2d2-5a3c49d686ec` repaired one tradeoff-contract error and emitted exactly one `most_robust` option, `option:hard-cap-fresh-maintenance-image-retention`. It removed the age exemption and treated count as a hard cap. Stage 5 rejected it and ended `insufficient_evidence`; no option was selected and no Stage 6 plan was produced. The exact blockers were an unproven reversal of the configured seven-day grace and `keep_local_count` semantics; no supported protection for the resolver-selected active image because cleanup does not receive that reference; an ID-count oracle that did not prove tag caps, aliases, protected tags, or active-reference preservation; and live Docker/container-reference behavior remaining downstream verification.
- Controller error: the Stage 5 findings were valid solution-design findings, but the controller routed them back into Stage 3 research retries instead of returning them to the original option author under the current optioning contract.
- Required action: after attempt 19's persisted evidence is authenticated, return the exact retained Stage 5 feedback to the same Stage 4 optioner and require genuinely distinct causal mechanisms or `insufficient_evidence`, not rhetorical breadth. Start a fresh option author only if the original session is unavailable.
- Closure proof: Stage 5 accepts a selected option whose causal-coverage matrix addresses bounded unique identities, existing age-grace behavior, active/protected/running/shared refs, visible overflow, and pre-build recovery semantics where claimed.

### BDS-013 — Maintenance prospective outcome oracle proves deletion, not bounded convergence

- Status: `moved_downstream`
- Priority: optioning, selection, planning, and outcome blocking; not Stage 3 blocking
- Objective impact: a plan could pass a one-burst regression while still allowing unbounded accumulation over larger or repeated bursts, which would reproduce the original root problem.
- Evidence: the latest parameterized harness improves the fail-first breadth—actual retained counts of 9 and 13 both exceed a computed bound of 5—but it does not establish the claimed safety or convergence contract. It merely labels five ordinary image IDs current/protected/active/running/shared without supplying those signals to production cleanup; uses `|S|=5` in both cases while never passing `L` to the production policy; verifies alias preservation only while all IDs remain; prints `preserve_S`, deletion safety, overflow, and convergence as literal `true`; computes the same `min(M, bound)` twice instead of invoking cleanup twice; and checks only that result field names exist. Thus the only substantive executable result is the broad bounded-retention failure, not protected-identity safety or a fixed point.
- Current attempt-44 evidence: the author materially improved the oracle by invoking production cleanup over mutable state and measuring `M`, protected set `S`, retained `R`, deletion scope, a second pass, and a subsequent burst. It still hardcodes `keep_local_days=0`, so it can pass without fixing the observed one-day-under-seven-day-grace mechanism; records but does not assert post-burst safety/bound/deletion scope or a final convergence pass; claims three aliases while only two repositories exist; checks only image-ID sets and can leave a partial alias family; and calculates protected overflow without requiring it to be emitted. Cleanup-before-build exhaustion is separately reproduced but not yet modeled as a causal facet, so optioning could omit recovery ordering.
- Current live-repair evidence: the retained early dossier corrected the historical policy input to one-day identities under the seven-day grace and ran four cleanup transitions. Its outcome oracle is still materially insufficient. `REPOSITORIES` contains exactly two repositories, so the advertised `aliases=3` relation silently executes two aliases and the two relations are actually 2/2 rather than 1/2. The test asserts first-pass `S <= R`, first-pass deletion scope, first/second equality, and post-burst/final equality, but bounds only first-pass `R` and checks alias integrity only in the first summary. Retained replay output shows post-burst/final `R=12` for bound 5 and `R=16` for bound 6; equality of those unbounded states was mislabeled as convergence. The report's independent outcome observable remains only `exit_code == 1`, and the verifier correctly reports `production_assertion_dataflow_unresolved` because the selected test passes the production callable through a helper rather than binding an authenticated direct production-output assertion.
- Required action: preserve this semantic finding, but do not send the maintenance case back through Stage 3 for it. Stage 4 must propose success properties and safety constraints that address bounded repeated behavior; Stage 5 must reject options that merely delete something or hide the symptom; Stage 6 must author exact executable checks; the outcome stage must run them against the implementation. Protection categories not exercised by the current mechanism must be scoped honestly to live verification rather than printed as established.
- Closure proof: the selected option and final plan cover multiple burst sizes or repeated cycles and specify executable assertions that unique managed identities are bounded by the selected ceiling plus reported required overflow; aliases count per identity; protected/active/running/shared/non-owned identities survive; and overflow/convergence is observable. Actual closure occurs only when post-change outcome evidence passes those checks.

### BDS-014 — Proof-adapter root diagnostics are hidden behind a six-error cascade

- Status: `verification_pending`
- Priority: throughput-degrading
- Objective impact: the same author receives secondary mechanism/falsification errors but not the direct proof-adapter rejection, encouraging blind rewrites and status downgrades instead of a small evidence-root correction.
- Evidence: attempt 36's persisted `proof_adapter_diagnostics` contains `authenticated_semantic_citation_invalid`, while its continuation prompt and top-level six validation errors do not. After that basis was corrected, the latest report's direct failure became `proof_adapter_unavailable:pytest_controlled_difference.v1`, but the author again received only downstream support/mechanism/falsification errors. The author alternated between an unavailable adapter and a direct-script pair that the verifier could not structurally bind, then downgraded the dossier despite established evidence.
- Required action: include content-bound proof-adapter diagnostics and their affected experiment/field in same-agent remediation feedback, while retaining downstream errors for independent verification. Generalize the message from adapter output; do not special-case this maintenance case.
- Local correction: targeted evidence-verification repairs now receive a separately labeled, content-addressed `proof_adapter_root_diagnostics` payload copied from the runner-owned verification receipt. It is explanatory feedback rather than a new hard gate, so an ancillary rejected claim does not reduce throughput; the ordinary verifier errors remain authoritative.
- Verification: focused feedback/repair-contract tests pass, including diagnostic de-duplication, affected experiment/adapter identity, immutable hashing, and prompt delivery. Ruff and diff checks pass.
- Closure proof: a focused test proves an invalid proof basis returns the root adapter diagnostic to the correcting author, and a live continuation repairs the basis without rediscovering it through downstream symptoms.

### BDS-015 — Causal-contrast predicates are conflated with operational outcome contracts

- Status: `stage_boundary_correction_in_progress`
- Priority: case-blocking, systemic
- Objective impact: a useful controlled difference is forced either to masquerade as the complete post-change success criterion or to fail proof validation. This rewards shallow predicates such as “fewer than 49” and prevents a general, independently executable outcome contract from coexisting with causal mechanism evidence.
- Evidence: `proof_adapters.base.build_receipt` evaluates `positive_outcome.predicate` against the challenge observation and stores it on the causal receipt. `_causal_proof_positive_contract` then unconditionally promotes that predicate into a post-change `causal_proof_predicate`. Yet `_positive_outcome_contracts` explicitly states that a different control input is causal boundary evidence, not by itself an original-input positive contract. For the maintenance case, the correct fresh/aged contrast yields the scalar `3`; the real success criterion is a relational, multi-cycle `M/S/L` property. Making the scalar predicate `< 49` would pass mechanics while remaining substantively shallow.
- Adversarial evidence retained under this issue rather than multiplied into new hardening tickets: an independent outcome experiment must not bootstrap the mechanism it claims to bind; `exit 0` alone can mean skip, xfail, zero collection, or an early return; and the existing monotonic AST taint accepts false flows such as `x=production(); x=0; assert x==1` or a production call in an untaken conditional. Printed literal booleans and a production call followed by an unrelated assertion are not outcome evidence. These are direct false-acceptance paths for shallow plans, not speculative PR-scope controls.
- Required action: retain the backward-compatible distinction between causal challenge and operational outcome, but move the mandatory future-solution contract out of Stage 3 readiness. A causal receipt must establish mechanism evidence without being forced to predict the final implementation. Stage 4 defines candidate success properties and safety constraints, Stage 5 reviews them adversarially, Stage 6 binds them to exact executable checks, and the outcome stage supplies post-change proof. A stronger research-authored operational contract may flow downstream when available, but it is not a universal prerequisite for optioning.
- Minimum authenticity boundary for the independent outcome: bind only to already-verified primary/root mechanism evidence that is independent of the outcome experiment; retain the exact failing assertion and call-to-observation-to-assertion path; accept only a deliberately restricted direct or immutable projection rather than the existing monotonic taint; reject overwrites, untaken conditionals, constant-dominated/reflexive/both-sides-tainted comparisons, unrelated failures, and production-symbol monkeypatching. Preserve exact selector/call-phase outcome telemetry so a later pass cannot be inferred from process exit alone. Do not require multiple scenarios generically: Stage 5 remains responsible for judging whether the authenticated assertion and scenario coverage are substantively sufficient for the problem.
- Local correction: `proof_adapter.positive_outcome.contract_role="causal_contrast"` is now an opt-in role. Its challenge predicate remains authenticated, runner-evaluated mechanism evidence but is not promoted as operational success; absence preserves legacy behavior. A separate `positive_outcome_contract.binds_hypothesis_id` path now mints a content-addressed runner receipt only when the outcome experiment is independent of mechanism selection, uses one exact retained pytest node, fails at the bound production-derived assertion before implementation, and has restricted unconditional single-assignment data flow without overwrite, constant domination, reflexive comparison, skip, xfail, parameterization, early return, or selected-symbol rebinding. Future closure remains explicitly `unverified_until_outcome_execution` until the same exact node exits zero and reports one pass. Full affected verification passed: 167 research-evidence tests plus one expected skip, 176 Stage-contract tests, 115 adjacent miner tests, 24 causal-proof tests, eight outcome-role tests plus one expected skip, focused compatibility tests, Ruff, and diff checks. This is deliberately a narrow authenticity boundary; Stage 5 still owns semantic sufficiency and scenario breadth.
- Closure proof: focused contract tests prove a contrast-only causal adapter can establish Stage 3 mechanism evidence without minting a post-change contract; legacy research outcome contracts remain compatible; and Stage 4-6 plus the outcome stage—not repeated Stage 3 turns—carry success semantics through exact planned and executed verification.

### BDS-017 - Controlled intervention values falsely change harness-consumer identity

- Status: `closed`
- Priority: case-blocking, systemic
- Objective impact: valid controlled experiments cannot authenticate the production mechanism, so a real root-cause result is represented as six downstream evidence failures and never reaches useful optioning.
- Evidence: attempt 44's baseline and challenge execute two exact pytest selectors in the same content-hashed harness and both directly call `runner_core.execution_backend.cleanup_local_maintenance_images`. Only `repo_root` changes from `inventory-age-1d` to `inventory-age-8d`. `_research_harness_dependency_edges` hashed the entire `ast.Call`, producing different dependency identities for the deliberate argument change; `_adapter_executed_consumer_receipt` then required byte-identical edge lists and returned no executed consumer. The function-reference AST is identical in both selectors, while the full call AST is not.
- Required correction: authenticate the selected function/reference AST rather than its intervention arguments. Preserve the entrypoint and source hashes, selected-test scan root, resolved production symbol, implementation touchpoint, and local dependency chain. Continue rejecting a challenge that calls a genuinely different symbol, merely imports/mentions the target outside the selected test, changes authenticated source bytes, or lacks command authorization.
- Local correction: `reference_ast_sha256` now hashes the selected call's function/reference node rather than the enclosing call arguments. No other dependency identity fields were relaxed.
- Verification: three focused harness-consumer tests pass. A model-free check against the exact retained maintenance harness resolves both fresh and aged selectors to one authenticated edge with identical reference hash `3255f55f...`; the different-production-symbol negative case remains rejected.
- Live closure proof: the next same-author dossier authenticated the fresh/aged production consumer and removed the six downstream harness/mechanism/falsification findings. Its two remaining findings concern the independent outcome contract and recovery-hypothesis binding, not controlled-call identity. The focused positive and different-symbol negative tests also pass.

### BDS-016 — Supervisor Docker prototype could be mistaken for system-authored implementation

- Status: `superseded_and_reconciled`
- Priority: objective-integrity, implementation-handoff blocking
- Objective impact: the shared remediation worktree formerly contained an uncommitted cleanup prototype in `runner_core.execution_backend`, its tests, and maintenance configuration. If research, planning, or outcome verification had read those overlay bytes as the target implementation, the pipeline could have appeared to solve its own case even though the solution was supplied outside the requested system workflow.
- Historical evidence: at branch HEAD `450a7332`, while research remained pinned to committed revision `51e35fbc`, the shared worktree diff added unique-ID capping, required/running/active protection, overflow reporting, and pre/post cleanup behavior. Those bytes predated the system-authored Stage 4–6 and `usertest implement` work.
- Required action: keep Stage 3, Stage 4, and Stage 5 pinned to their authenticated clean target revision/workspaces. When the plan is ready, run `usertest implement` in its own isolated target workspace and require its own patch, commit, tests, original-scenario replay, and live Docker evidence. Treat the shared prototype only as supervisor diagnostic material; compare and reconcile it after the system implementation without silently copying or counting it.
- Current proof: the system path used the isolated retained workspace `U:\usertest_backlog_component_validation\runs\usertest_implement\_workspaces\usertest_20260715T143437Z_codex_0`, clean researched base `51e35fbcb50151590604611f10910a7fd13660e4`, branch `backlog/03f0d43eb78e`, exact Codex author session `019f6635-03a2-7f60-acab-3d97e277332f`, and eight commits ending at `e3f988b8e391ad020e746d3bc3937fca53cf6363`. PR #213 and correction PR #215 then supplied the verified system-authored implementation and live outcome evidence. Integration commit `e2614d225b96ff7d567751335bda2deafeec133f` explicitly selected that remote history as authoritative and did not reintroduce the supervisor prototype's age-window exception, `active_image_refs`, proactive running-container exemption, managed-prefix families, owner-label inventory, or local summary contract. Only the separately justified configured retention count of two and sandbox lifecycle controls remain.
- Closure proof: the implementation receipt identifies the system author/session, clean base, produced commits, tests, original-scenario and live results; `e2614d22` records the semantic reconciliation; and the obsolete supervisor prototype is absent from the integrated product diff. This issue is closed unless those superseded semantics reappear.

### BDS-008 — Superseded session acquisition can retry without a useful bound

- Status: `open`
- Priority: operational, nonblocking for current case
- Objective impact: wastes compute and supervision attention without producing backlog throughput.
- Evidence: obsolete run `stages456_maintenance_image_live_terra_20260714T102700Z` reached roughly 409 session-acquisition attempts after a later run had already superseded it; its exact process tree was stopped.
- Required action: add this as automated evidence/atom after the current case reaches disposition, then process it normally. Independently ensure superseded runs stop acquiring sessions.
- Closure proof: a superseded stage exits deterministically and the issue is represented in the automated case graph rather than only in this ledger.

### BDS-009 — Repeated runs can accumulate Docker images and local workspaces

- Status: `operationally_recovered_root_cause_confirmed_monitoring_required`
- Priority: operational, pipeline-blocking if recurrence is not detected early
- Objective impact: Docker host-allocation growth exhausted the host system drive and prevented evidence replay, halting the pipeline and risking unrelated host operations.
- Pre-recovery evidence: at 14:53–15:00 America/New_York on 2026-07-14, C: had only 0.225–0.34 GiB free of 487.5 GiB. `C:\Users\jason\AppData\Local\Docker\wsl\disk\docker_data.vhdx` occupied 192.755 GiB on the host, while read-only inspection inside the mounted Docker disk found only about 5.8 GiB used. The retained usertest workspace root was 8.606 GiB logical, so workspace cleanup was not the primary explanation for the host-space incident.
- Object-retention evidence: the 2026-07-13 cleanup receipt records 31 stopped containers, 49 obsolete image IDs, and 2.426 GB of build cache removed. Immediately afterward Docker contained zero containers, four unique image IDs, 3.554 GB of reported image data, and zero build cache, but the VHDX still occupied exactly 206,968,979,456 bytes (192.755 GiB). The bounded live inventory therefore did not cause the later host-file size; deletion had made blocks free inside the guest without returning their allocation to C:.
- Churn evidence: retained BuildKit history contains 50 builds from 2026-07-08 through 2026-07-13; 44 are explicitly maintenance-named, 30 originated under `runs/usertest_implement`, and 46 report only seven cached steps. The current maintenance image includes a 1.67 GB dependency layer and a 1.69 GB source-dependent environment-seeding layer; its later workspace deletion is an 8.19 KB whiteout layer and does not erase lower-layer bytes. Windows VHDMP summaries record 75.786 GiB of writes on July 8, 139.092 GiB on July 9, 11.981 GiB on July 10, 34.738 GiB on July 11, 30.907 GiB on July 12, 2.810 GiB on July 13, and 0.467 GiB before compaction on July 14. These figures are I/O volume, not unique allocation, but they establish that repeated substantive image rebuilds were quantitatively capable of driving the high-water allocation and that most write pressure preceded the final two days.
- Reclamation proof: Docker's own shutdown log at 15:00:01 recorded `host-side disk file size 192.74 GB, VM-side disk usage 5.76 GB, threshold: 16 GB`; 9.8 seconds later it recorded `data disk vhdx compacted`. Windows VHDMP events 50 and 51 independently record the compact operation and its success. Afterward the non-sparse VHDX occupied 11.492 GiB and the first post-restart C: measurement showed 181.589 GiB free. The approximately 181.263 GiB VHDX reduction accounts for essentially the entire approximately 181.364 GiB host free-space recovery, with the Docker inventory still at zero containers, four images, zero volumes, and zero build cache.
- Attribution boundary: a retained 2026-07-08 12:47 artifact already measured the VHDX at about 200.0 GiB, proving that it was large before the later live-inventory cap. Compactions occurred later on July 8, followed by the heaviest recorded Docker write period, but no post-compaction VHDX-size snapshot was retained. It is therefore not possible to divide the 192.755 GiB high-water exactly between older allocation and the July 8–12 rebuild burst. It is proven that the small bounded state did not grow the file after July 13; it is also proven that recent maintenance-image churn materially contributed write pressure. Other C: writers may have consumed the remaining headroom, but no retained C:-free snapshot at the cleanup boundary can quantify them.
- Runtime consequence: the computer/Docker restart restored engine responsiveness after compaction. The earlier `host.docker.internal` DNS startup failure was a separate engine-start failure and is not attributed to disk pressure without evidence. No image, container, cache, workspace, VHDX block, or evidence artifact was deleted, pruned, moved, or manually compacted during the follow-up investigation.
- Approval boundary: the user explicitly requires approval before removal. No prune, move, compaction, or deletion may occur until an exact proposed action and preservation boundary are approved.
- Required action: measure VHDX host allocation, VM-side used space, Docker object inventory, and C: free space at bounded run checkpoints; retain Docker compaction events; and supervise the next pipeline sections at small scale. Any future removal or deliberate compaction remains separately approval-gated. The operational control should detect the live-use/host-allocation divergence without blocking useful throughput merely because cumulative I/O is high.
- Closure proof: two consecutive supervised pipeline cycles retain bounded live Docker objects, do not approach unsafe C: headroom, and either keep host allocation bounded or demonstrably reclaim internally free blocks through the normal Docker compaction lifecycle. This remains an operational qualification item, not a reason to halt unrelated analysis.

## Verification-pending fixes

### BDS-001 — Stage 3 treated future design choices as material research unknowns

- Status: `verification_pending`
- Evidence: researcher repeatedly demanded an approved future ceiling/interface and completed post-change proof before optioning.
- Local correction: mission and guidance now distinguish empirical unknowns from future design parameters and permit fail-first/parameterized outcomes. Five focused prompt-guidance tests pass.
- Closure proof: the maintenance dossier advances on established causal evidence while leaving the exact ceiling/interface to Stage 4, with no false material unknown.

### BDS-002 — Independent-feedback lineage could bind to a stale source attempt

- Status: `verification_pending`
- Evidence: historical attempt 22 omitted an existing source error when downstream feedback arrived.
- Current evidence: the maintenance resume helper hash-binds attempt 36's dossier/session/auth/revision, but the referenced live workspace has since accumulated 19 untracked paths and harness edits from attempts 37–40. Resuming it as an exact attempt-36 workspace would conflate objective-best claims with later overlay bytes. Restoring attempt-36 bytes would instead discard later authored work.
- Local correction: future feedback carries the stable union of existing and independent errors; legacy records remain unchanged. Focused and affected-module suites passed (269 tests), and the retained historical dossier verifies without mutation. The throw-away resume helper now represents attempt 36's immutable authored claims separately from a freshly observed current-workspace overlay, hashes the full tracked binary diff, porcelain-v2 status, and every untracked path/hash/size, includes that manifest in a content-addressed continuation frontier and independent-feedback hash, and recomputes both immediately before model invocation. Two model-free preflights, syntax compilation, and Ruff passed. No model was invoked during this repair.
- Required action: execute the content-bound continuation with both expected hashes and verify the persisted next attempt records the immutable source attempt and observed overlay as distinct provenance.
- Closure proof: the next live continuation records before/after errors against the actual source attempt, records the current overlay manifest separately, preserves all later authored artifacts, and passes lineage validation.

### BDS-003 — Stage 4 helper falsely required research and optioning workspaces to match

- Status: `verification_pending`
- Evidence: the helper conflated independent research, updated planning, and original optioner workspaces.
- Local correction: each workspace is now validated through its own revision/receipt/cleanliness semantics; syntax, Ruff, and model-free preflight passed.
- Closure proof: live Stage 4 continuation accepts the valid independent workspaces and invokes only the original optioner session.
